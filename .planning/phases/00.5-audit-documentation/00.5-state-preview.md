# chezmoi state preview - Phase 0 input

**Status:** SCAFFOLD (Mac personal populated by Plan 03; Mac work populated by Plan 06)

This document is empty by design. Plan 03 captures Mac personal state into
the `Mac personal` section below; Plan 06 captures Mac work state into the
`Mac work` section and computes deltas.

---

## Purpose

Phase 0's structural refactor needs to know **what chezmoi currently has in
state** before any source-shape change touches `run_once_` / `run_onchange_`
hashes or `entryState` tracking. This document is Phase 0's input artifact —
it gives Phase 0 the per-machine `scriptState` + `entryState` surface
without requiring a second discovery pass after this phase closes.

Phase 0.5 does NOT modify state. This is documentation/preview only.

## Surprises to Surface

These are the non-obvious behaviors Plan 03 + Plan 06 should highlight when
populating the per-machine sections below. They live in this scaffold because
they affect interpretation, not because either machine has been captured yet.

1. **`home/scripts/generate-gpg-key.sh` is a PLAIN target file, NOT a
   `run_once_` script.** It is applied to `~/scripts/generate-gpg-key.sh`
   like any other tracked file. Phase 0's re-fire concern (deleting a
   `run_once_` source leaves the script hash in `scriptState`, so it can
   re-fire on a fresh apply) **does NOT apply here.** Deleting the source
   removes the destination on the next `chezmoi apply`. No
   `chezmoi state delete` action is needed for this file.

2. **Externals refresh weekly via `refreshPeriod = "168h"`** in
   `.chezmoiexternal.toml`. `chezmoi diff` without `-x externals` will show
   100+ lines of oh-my-zsh and tpm churn within the refresh window. Every
   phase-gate diff uses `chezmoi diff -x externals` (Pitfall A in RESEARCH).
   Capture commands in this document MUST also exclude externals so we don't
   pollute the preview with cache churn.

---

## Mac personal

**Captured:** _(YYYY-MM-DD HH:MM — Plan 03 fills via `date`)_
**chezmoi version:** _(Plan 03 fills via `chezmoi --version`)_

### scriptState entries

| SHA prefix | Script name | runAt |
| ---------- | ----------- | ----- |
| _(Plan 03 populates from `chezmoi state get-bucket --bucket=scriptState`)_ | | |

### entryState — applied files Phase 0 cleanup must consider

When this table is populated (Plan 03), explicitly call out:

- **`~/.config/flameshot/flameshot.ini`** — AUD-03 input. Source removed in
  Plan 04; destination cleanup also handled by Plan 04 (Pitfall C — chezmoi
  does not auto-remove destination on source delete by default; entryState
  carries it until apply with `--remove` or manual `rm`).
- **`~/scripts/generate-gpg-key.sh`** — Phase 0 input (NOT a `run_once_`).
  See "Surprises to Surface" #1. Confirm presence in entryState; absence in
  scriptState.

| Destination path | Type | Notes |
| ---------------- | ---- | ----- |
| _(Plan 03 populates from `chezmoi state get-bucket --bucket=entryState`)_ | | |

## Mac work

**Captured:** _(YYYY-MM-DD HH:MM — Plan 06 fills, requires physical or SSH access to Mac work)_
**chezmoi version:** _(Plan 06 fills)_

### scriptState entries

| SHA prefix | Script name | runAt |
| ---------- | ----------- | ----- |
| _(Plan 06 populates)_ | | |

### entryState — applied files Phase 0 cleanup must consider

Same call-outs as Mac personal apply if symmetric. Highlight any
work-machine-only entries (e.g., work-specific configs in entryState that
Mac personal does not track).

| Destination path | Type | Notes |
| ---------------- | ---- | ----- |
| _(Plan 06 populates)_ | | |

## Cross-machine deltas

_(Paragraph placeholder — Plan 06 populates after Mac work capture.
Examples to surface: scripts present in Mac personal scriptState but absent
on Mac work (or vice versa); entryState files that differ between machines;
any `run_once_` scripts whose `runAt` differs by months (suggests one
machine missed an intentional re-fire). These deltas feed Phase 0's
per-machine cutover ritual.)_

---

*Scaffold created by Plan 00.5-01 (Wave 0 harness). Populated by Plans 00.5-03 (Mac personal) and 00.5-06 (Mac work).*
