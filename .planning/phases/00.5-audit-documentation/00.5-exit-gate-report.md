# Phase 0.5 Exit Gate Report

**Status:** BOTH MACS PASS (Mac personal EMPTY ✓; Mac work via single justified escalation ✓).
**Closed:** 2026-05-29

This report is the load-bearing artifact for Phase 0.5 close. Plan 06 Task 1 reconciled Mac personal drift (DEBUG=1 + Brother iPrint state-forge); Task 4 (this section) populates Mac personal exit status and scaffolds Mac work. Task 6 finalizes after Teague's Mac work paste-back.

---

## Mac personal

- **Captured:** 2026-05-29 ~09:50 CDT (post-rebase apply)
- **chezmoi version:** v2.70.4, commit `64583685c5eb36e10670bad076d5406a08baf751` (Homebrew, 2026-05-19)
- **Source SHA at gate verification:** `f59d8fe` (Plan 06 Task 3 Mac work capture commit)
- **`chezmoi diff -x externals`:** **EMPTY ✓**
- **Drift reconciliation history:** see `00.5-drift-reconciliation.md`
  - Original Plan 06 Task 1 (2026-05-27): `.zshrc` DEBUG=1 reverted via `chezmoi apply --force ~/.zshrc` (decision 1A); Brother iPrint entryState SHA forged after filesystem verification (decision 2A)
  - Plan 06 Task 4 follow-up (2026-05-29): rebase onto `origin/master` pulled `fa0a669 chore: discord is personal only now` which mutated `packages.yaml` and triggered a `02-install-packages.sh` run_onchange re-fire (Pitfall D). Resolved via `chezmoi apply -x externals` — `brew bundle` no-op'd (81 deps all "Using ..."), entryState updated, diff returned to empty.
- **`quick.sh` (Wave 0 assertions, run 2026-05-29):** **12 PASS / 0 PENDING / 0 FAIL**
  - AUD-04 .gitattributes ✓ (2 checks)
  - AUD-05 docs ✓ (4 checks: conventions.md, dot_topics.md, content greps)
  - AUD-03 flameshot orphan absent ✓
  - AUD-01/02 packages.yaml ✓ (2 checks: YAML validity, shottr present)
  - SS-01 Shottr installed via brew --cask ✓
  - Wave 0 scaffolds present ✓ (2 checks)
- **`full.sh --strict`:** _(run before final commit if Teague requests strict-mode rerun; quick.sh already covers the load-bearing assertions)_

## Mac work

- **Captured:** 2026-05-29 ~14:36 CDT (post-apply + cleanup, pre-final-capture)
- **chezmoi version:** v2.69.4, commit `c4c669c9f2f329233a85802014d26fba3c58a4a4` (Homebrew, 2026-02-11) — **4 months stale vs Mac personal's 2.70.4** (Phase 0 follow-up #7)
- **CRLF pre-check (Task 2):** **LF-clean ✓** (Mac work `git ls-files --eol -- 'home/**/*.tmpl' | grep -v 'i/lf'` returned empty)
- **State capture (Task 3):** ✓ committed in `f59d8fe`, populated `00.5-state-preview.md` Mac work section and Cross-machine deltas in Task 4

### Task 5 reconciliation summary

Three mid-flight reconciliations were required to converge on the gate (full per-entry detail in `00.5-drift-reconciliation.md` Mac work section):

1. **`localstack` → `localstack-cli` hot-patch (fix-source).** Plan 05's candidate review misdiagnosed `localstack-cli` as MISNAMED; reality is that `localstack` is deprecated upstream and `localstack-cli` is the maintained formula. Mac work's existing `localstack-cli` install blocked `brew bundle` from installing deprecated `localstack` over the same symlink. Source-reverted in commit `3725e90`.
2. **Flameshot residue cleanup (fix-machine).** `rm -rfv ~/.config/flameshot/` removed the destination. `chezmoi state delete` invocations errored at the zsh layer (chezmoi 2.69.4 vs 2.70.4 CLI delta or `=` parser interaction — Phase 0 follow-up #7). Non-blocking: diff reads source vs destination, both absent, diff silent for flameshot.
3. **`~/bin/start-aws-mcp.sh` removed via `exact_bin` directive (Phase 0 input).** Initial hypothesis (auto-removal from entryState orphan) was wrong. Root cause: `home/exact_bin/` in source carries the `exact_` chezmoi prefix, which enforces that `~/bin/` contains ONLY files in source. Mac work had Bluebeam-specific `start-aws-mcp.sh` in `~/bin/` (never in chezmoi source); every apply removed it. `chezmoi state delete` doesn't help — the directive is the mechanism, not entryState. Script restored from Task 5 capture and **moved to `~/.local/bin/start-aws-mcp.sh`** (non-managed dir, first on PATH via mise). NOT added to chezmoi source — Bluebeam-AWS-tooling-specific (same Phase 0 follow-up #6 axis as `.zshrc` NODE_EXTRA_CA_CERTS). New Phase 0 follow-up #9 captured: rename `exact_bin` → `bin` OR standardize on `~/.local/bin/` for personal/employer-local tooling.

### Justified escalation — only entry that remains in `chezmoi diff -x externals`

**`~/.zshrc` line — `export NODE_EXTRA_CA_CERTS=/Users/jteague/.certs/CAcerts.pem`** is **escalated to Phase 0** rather than fix-source. The line is **Bluebeam-employer-specific** (corporate Node TLS cert path), not all-work-machines-applicable. Gating under existing `{{ if not .personal }}` would carry Bluebeam config to every future work-axis machine. Phase 0's role taxonomy work needs to introduce an employer/site axis before this gets templated.

Until Phase 0 lands the employer axis:
- Mac work `chezmoi apply` will keep prompting to remove this line; Teague keeps skipping
- `chezmoi diff -x externals` on Mac work will show this single line as drift
- This satisfies the Plan 06 success criterion "empty OR only justified escalations"

### Exit gate status

- **`chezmoi diff -x externals` FINAL:** Only the `.zshrc` NODE_EXTRA_CA_CERTS line (matches documented escalation) ✓
- **Final gate capture:** `.planning/phases/00.5-audit-documentation/captures/mac-work-task5.txt` (committed `8a7c6d0`)
- **`full.sh` on Mac work:** NOT RUN (Teague did not opt in; quick.sh on Mac personal covers the load-bearing AUD assertions, all 6 requirements are global-source assertions)
- **GATE PASS via justified escalation ✓** (Teague confirmed 2026-05-29 ~15:15 CDT)

## Phase 0 input summary

This section enumerates the artifacts Phase 0 inherits from 0.5. All paths are repo-relative.

- **state-preview:** `.planning/phases/00.5-audit-documentation/00.5-state-preview.md` — both Macs + cross-machine deltas. Phase 0's `chezmoi init --apply` rehearsal plan should sanity-check the 5 shared SHAs (brew/darwin-configure/install-topics/prep-clean-machine) remain stable through the refactor.
- **state capture artifacts:** `.planning/phases/00.5-audit-documentation/captures/mac-work-state.txt` (raw JSON dumps committed `f59d8fe`). Mac personal raw capture lives only in `00.5-state-preview.md` tables — Phase 0 can re-capture if it needs the raw form.
- **packages candidate decisions:** `.planning/phases/00.5-audit-documentation/00.5-packages-candidates.md` (Plan 05 source list with two-signal verification + human-reviewed decisions)
- **drift reconciliation log:** `.planning/phases/00.5-audit-documentation/00.5-drift-reconciliation.md` (Mac personal entries from Plan 06 Task 1; any Mac work entries from Task 5)
- **flameshot baseline:** `.planning/phases/00.5-audit-documentation/00.5-04-flameshot-baseline.md` — verbatim `.ini` content + SHA + mode preserved. Phase 0's role=dev+os=linux+not-wsl re-staging pulls from this.
- **docs flagged inconsistencies:** `docs/conventions.md` § 10 (6 known inconsistencies surfaced during AUD-02 LIGHT scope: `executable_` prefix outliers, OS-routing asymmetries, `packages.yaml` cross-cutting structure smells, `.DS_Store` at root)
- **Phase 0 follow-ups captured during 0.5 execution:**
  1. `chezmoi state dump` is canonical clean-check for stale entryState (Plan 04 finding)
  2. `chezmoi apply --dry-run --verbose` exits nonzero on interactive TTY prompts (Plan 05 finding)
  3. `mas list` invisibility for Apple-ID-mismatched apps (Brother iPrint case; Plan 06 Task 1) — Phase 0 needs an Apple ID provenance audit step OR `mas install` should pre-check `/Applications/<App>.app` existence
  4. `mas install` non-interactive failure mode — `03-mas.sh` improvement Phase 0 candidate
  5. state-forge pattern (symmetric inverse of state-delete) documented as legitimate when underlying reality verified
  6. AUD-02 LIGHT remainder — `docs/conventions.md` § 10 captures the 6 deferred inconsistencies for Phase 0
  7. `generate-gpg-key.sh` is NOT `run_once_` — Phase 0's re-fire concern does not apply (state-preview Surprise #1)
  8. Loader-order delta from RESEARCH — actual `dot_zshrc.tmpl` is `path → compinit → completion → middle` (RESEARCH said `path → middle → completion`); docs reflect reality

## Phase 0.5 Attestation

- **Closed:** 2026-05-29
- All AUD-01..05 + SS-01 requirements verified ✓
- Both-Mac exit gate: PASS (Mac personal SHA at close: `8a7c6d0` (post-apply, post-discord-rebase, post-localstack-fix); Mac work SHA at close: `8a7c6d0` (same — Mac work tracked through all source-tree commits via Task 3/4/5 captures))
- Phase 0 inputs: documented in "Phase 0 input summary" section above (8 items + 1 escalation = 9 items for Phase 0 to consume).

### Anti-scope-creep verification

- [x] No taxonomy/role/personal axis changes (Phase 0 territory) ✓ — only the existing `.darwin.personal.casks` block was used (discord move came from Teague's prior commit, not this phase); NODE_EXTRA_CA_CERTS gating ESCALATED rather than templated (avoided introducing an employer axis)
- [x] `generate-gpg-key.sh` NOT deleted (Phase 0 owns) ✓ — confirmed present in both Macs' entryState (Mac personal: file SHA 79b9131c; Mac work: identical SHA)
- [x] No `.chezmoiignore` templating (Phase 0 owns) ✓ — `.chezmoiignore` not even created
- [x] No new `promptStringOnce` prompts (Phase 0 owns) ✓
- [x] No VaultWarden / secret-plane work (Phase 1 owns) ✓
- [x] `home/exact_bin/` NOT modified (Phase 0 follow-up #9 owns the rename decision) ✓ — Mac work resolved via destination move to `~/.local/bin/`, not via source-tree directive change
