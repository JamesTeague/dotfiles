---
phase: 00.5-audit-documentation
plan: 04
subsystem: infra
tags: [flameshot, chezmoi-state, entryState, orphan-removal, dead-config, AUD-03, mac-personal]

requires:
  - phase: 00.5-audit-documentation
    provides: "Wave 0 harness (quick.sh AUD-03 section asserting source dir absent); state-preview Pitfall C analysis (00.5-state-preview.md + 00.5-RESEARCH.md); docs/conventions.md §9 'chezmoi diff -x externals' canonical clean-check"
provides:
  - "home/private_dot_config/flameshot/ source directory deleted from git tree"
  - "~/.config/flameshot/ destination directory removed on Mac personal"
  - "chezmoi entryState on Mac personal no longer tracks /Users/jteague/.config/flameshot or /Users/jteague/.config/flameshot/flameshot.ini (surgical delete via `chezmoi state delete --bucket=entryState --key=...`)"
  - "Reversibility evidence — 00.5-04-flameshot-baseline.md preserves source mode/size/sha/contents for Phase 0 role=dev+os=linux+not-wsl re-staging"
  - "Empirical finding (research delta): post-source-delete, `chezmoi managed | grep flameshot` is SILENT — chezmoi managed reads from current source tree, not entryState. entryState is the ONLY trace of the orphan once source is deleted; Pitfall C's predicted 'orphaned diff entry' did NOT manifest in `chezmoi diff -x externals` either. State hygiene is still load-bearing for Phase 0's `chezmoi init --apply` rehearsals (stale entryState would break clean-machine assumptions)."
affects:
  - "00.5-06-exit-gate (AUD-03 row flips to PASS; Mac personal flameshot contribution to `chezmoi diff -x externals` is zero — only pre-existing `.zshrc` DEBUG=1 drift remains, which Plan 06 owns)"
  - "phase-0-refactor (when role=dev + os=linux + not-wsl flameshot block is re-staged, contents in 00.5-04-flameshot-baseline.md are the verbatim source for the Linux-appropriate variant; `savePath=/Users/jteague/Screenshots` was the original Mac-path smell that justified role/OS gating)"

tech-stack:
  added: []
  patterns:
    - "Surgical chezmoi state cleanup pattern: `chezmoi state delete --bucket=entryState --key=<absolute-destination-path>` for each tracked entry (dir + file) — avoids the cross-plan side effects of `chezmoi apply` when only ONE entry needs reconciliation and other drift (e.g., `.zshrc`) is owned by a different plan."
    - "Reversibility-via-baseline-notes pattern: before destructive ops, capture mode/size/sha/contents into `.planning/phases/XX/<plan>-baseline.md` — git history covers source recovery, but destination state + entryState fingerprint are NOT in git and must be captured separately."

key-files:
  created:
    - ".planning/phases/00.5-audit-documentation/00.5-04-flameshot-baseline.md — pre-delete snapshot (Task 1); 87 lines; preserves .ini contents verbatim + entryState SHA + mode + sizing"
    - ".planning/phases/00.5-audit-documentation/00.5-04-SUMMARY.md — this file"
  modified:
    - "(git tree) home/private_dot_config/flameshot/flameshot.ini — DELETED via `git rm` (Task 2)"
    - "(Mac personal destination, not in git) ~/.config/flameshot/ — DELETED via `rm -rf` (Task 3)"
    - "(Mac personal chezmoistate.boltdb, not in git) entryState bucket — 2 keys deleted via `chezmoi state delete` (Task 3)"

key-decisions:
  - "Executed Option A-modified (state-only) instead of canonical Option A (`chezmoi apply`) per explicit Teague approval ('approved state-only'). Rationale: Mac personal has pre-existing `.zshrc` DEBUG=1 drift (Pitfall B in RESEARCH; Plan 06 territory). A full `chezmoi apply` would reconcile that drift as a side effect, polluting Plan 04's commit scope and stealing reconciliation work from Plan 06 where it's properly tracked."
  - "Used `chezmoi state delete --bucket=entryState --key=<path>` (singular `delete`, not `delete-bucket`) for both the dir key AND the file key — chezmoi 2.70.4 entryState stores them as separate entries (verified in baseline notes). Skipping either would leave a half-orphan."
  - "Empty (--allow-empty) git commit for Task 3 — operations are filesystem + boltdb only, no source-tree changes. Commit body documents the exact 3 ops + verification results so the audit trail is complete despite zero diff. Pre-existing unrelated working-tree changes (config.json, nvim plugin files) were NOT staged — kept Plan 04 surgical."
  - "Did NOT run `chezmoi apply` — confirmed `.zshrc:80` `DEBUG=1` line is untouched after all Task 3 ops. Plan 06's exit-gate work owns reconciliation."

patterns-established:
  - "When destination + state cleanup is needed but other drift exists that's owned by a different plan: state-only surgical cleanup > `chezmoi apply`. Cite this plan + Teague's 'approved state-only' decision when the pattern recurs."
  - "Empty commits are valid task anchors when operations are entirely out-of-tree (destination filesystem + chezmoi state). The commit body becomes the audit trail."
  - "chezmoi state entries for a directory + its file are SEPARATE keys; cleanup must delete both. Check via `chezmoi state get-bucket --bucket=entryState | grep -A2 <path>` to enumerate before deletion."

requirements-completed: [AUD-03]

duration: 25 min
completed: 2026-05-27
---

# Phase 00.5 Plan 04: Flameshot Removal Summary

**Orphaned `home/private_dot_config/flameshot/` source directory (single 147-byte `flameshot.ini`) deleted from git tree AND `~/.config/flameshot/` destination removed on Mac personal with surgical entryState cleanup — AUD-03 closed without touching pre-existing `.zshrc` DEBUG=1 drift (Plan 06 territory preserved).**

## Performance

- **Duration:** 25 min (spanned a checkpoint pause for Teague approval)
- **Started:** 2026-05-27T21:43:22-05:00 (Task 1 commit)
- **Completed:** 2026-05-27T22:08:39-05:00 (Task 3 commit)
- **Tasks:** 3 (2 auto + 1 human-verify checkpoint)
- **Files modified:** 1 source-tree deletion + 1 destination deletion + 2 entryState keys deleted + 2 planning notes files

## Accomplishments

- AUD-03 requirement complete — flameshot orphan eliminated from source tree, destination, and chezmoi state on Mac personal
- Reversibility baseline captured (`00.5-04-flameshot-baseline.md`) — Phase 0's role=dev+os=linux+not-wsl flameshot re-staging can pull verbatim .ini content + know the original `savePath=/Users/jteague/Screenshots` was the Mac-path smell that justified role/OS gating
- `quick.sh` Wave 0 harness now reports `12 PASS / 0 PENDING / 0 FAIL` (AUD-03 flipped from PENDING to PASS)
- Plan 06's `.zshrc` reconciliation territory left intact — `DEBUG=1` line at `~/.zshrc:80` confirmed untouched post-cleanup

## Task Commits

1. **Task 1: Confirm pre-state and capture baseline** — `fd18ccd` (docs)
2. **Task 2: Delete source directory + commit** — `5ebf307` (refactor / AUD-03 source delete)
3. **Task 3: Clean destination + entryState (Mac personal, state-only)** — `2dd08a2` (chore, --allow-empty; state + destination only)

Note: Task 3 is an empty git commit by design — all operations were on the Mac personal destination filesystem and chezmoistate.boltdb (neither of which is in git). The commit message body is the audit trail.

## Files Created/Modified

**Source tree (git):**
- `home/private_dot_config/flameshot/flameshot.ini` — DELETED (was 147 bytes, mode 0644)

**Planning artifacts (git):**
- `.planning/phases/00.5-audit-documentation/00.5-04-flameshot-baseline.md` — CREATED (87 lines; pre-delete snapshot; reversibility evidence)
- `.planning/phases/00.5-audit-documentation/00.5-04-SUMMARY.md` — CREATED (this file)

**Mac personal destination (NOT in git):**
- `~/.config/flameshot/` — DELETED (contained 147-byte `flameshot.ini` matching source SHA `f6d604ab2cf27d1a6f4a77582cf26671557348f5b97da5df8e209513395d7e44`)

**Mac personal chezmoistate.boltdb (NOT in git):**
- `entryState` bucket: key `/Users/jteague/.config/flameshot` (type=dir, mode=2147484141) — DELETED
- `entryState` bucket: key `/Users/jteague/.config/flameshot/flameshot.ini` (type=file, mode=420, contentsSHA256=f6d604ab…) — DELETED

## Decisions Made

1. **State-only cleanup over `chezmoi apply`** — Teague explicitly approved Option A-modified ("approved state-only") to avoid Plan 06 side effects. `chezmoi apply` would reconcile pre-existing `.zshrc` DEBUG=1 drift, which is Plan 06's exit-gate territory. Surgical `chezmoi state delete` per key was the minimal-blast-radius alternative.
2. **Two-key delete (dir + file)** — chezmoi 2.70.4 stores directory entries and file entries as separate entryState keys. Baseline notes enumerated both; cleanup deleted both. Skipping either leaves a half-orphan.
3. **Empty (--allow-empty) Task 3 commit** — All Task 3 operations were out-of-tree (destination filesystem + boltdb). Empty commit anchors the audit trail in git history without staging unrelated pre-existing working-tree changes (`config.json`, nvim plugin files).
4. **Did NOT touch `.zshrc`** — Post-cleanup verification confirmed `~/.zshrc:80` `DEBUG=1` line is untouched. Reconciliation deferred to Plan 06 as designed.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 4 deferred to user — handled via checkpoint, not auto-fix] Switched Task 3 from canonical Option A to state-only Option A-modified**

- **Found during:** Task 3 (human-verify checkpoint)
- **Issue:** Plan's recommended Option A (`rm -rf ~/.config/flameshot/ && chezmoi apply`) would have reconciled pre-existing `.zshrc` DEBUG=1 drift as a side effect, polluting Plan 04's scope and stealing work from Plan 06's exit-gate reconciliation.
- **Resolution:** Surfaced at checkpoint as part of human-verify flow. Teague responded "approved state-only" — execute destination `rm -rf` + surgical `chezmoi state delete` per key, no `chezmoi apply`.
- **Files modified:** No additional files vs original plan — same scope (destination + state), narrower mechanism.
- **Verification:** `~/.zshrc:80` `DEBUG=1` line confirmed untouched; `chezmoi diff -x externals | grep -i flameshot` is empty; `chezmoi state get-bucket --bucket=entryState | grep -i flameshot` is empty.
- **Committed in:** `2dd08a2` (Task 3 commit message body documents the modified ops explicitly)

---

**Total deviations:** 1 (handled via checkpoint per design — NOT an auto-fix; was the gated decision the checkpoint existed to surface)
**Impact on plan:** Zero scope change. Mechanism narrowed to keep Plan 04 surgical and preserve Plan 06's reconciliation territory.

## Research vs Reality Delta

The plan was built on RESEARCH Pitfall C: "source delete alone leaves an orphan in chezmoi state and a non-empty diff — destination cleanup is REQUIRED." Empirical observations on Mac personal during execution:

| Predicted (Pitfall C)                                          | Observed (Mac personal, chezmoi 2.70.4)                  | Delta                                                                                          |
| -------------------------------------------------------------- | -------------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| Post-source-delete, `chezmoi managed` shows flameshot as orphan | `chezmoi managed | grep flameshot` returns EMPTY after Task 2 | `chezmoi managed` reads from current source tree, not entryState — orphan invisible to this command |
| Post-source-delete, `chezmoi diff -x externals` shows flameshot orphan entry | `chezmoi diff -x externals | grep -i flameshot` returns EMPTY after Task 2 | Mac personal's default `chezmoi diff` config does not surface entryState orphans without an explicit reapply context |
| entryState retains the orphan keys                             | ✓ CONFIRMED — two keys present (`flameshot` dir + `flameshot.ini` file) | RESEARCH correct on this dimension                                                             |

**Why this still matters:** State hygiene is load-bearing for Phase 0's `chezmoi init --apply` rehearsals on fresh-machine simulations — a stale entryState entry would cause a clean-machine bootstrap to behave differently than a state-clean one. The observed silence of `chezmoi managed` / `chezmoi diff` is a discoverability issue, not a correctness one. Plan 06's exit-gate should consider adding a `chezmoi state dump | grep <expected-clean-paths>` step to catch this class of silent state debt.

**Forwarded as Phase 0.5 closure note:** Plan 06 should grep `chezmoi state dump` for any path Phase 0 plans to delete-and-not-replace, since `chezmoi managed` / `chezmoi diff` may not surface those entries.

## Issues Encountered

None during execution. The pre-existing working-tree changes (`.planning/config.json`, two nvim plugin files) were noted at Task 3 commit time and explicitly kept OUT of the Task 3 commit (`--allow-empty` instead of staging) — they belong to other work streams.

## Authentication Gates

None.

## Next Phase Readiness

- AUD-03 PASS — flameshot orphan fully eliminated on Mac personal (source + destination + state).
- Plan 06 (exit-gate) is now unblocked for AUD-03 on Mac personal. Mac work is not in scope for Plan 04 (flameshot was Mac-personal-specific; never tracked on Mac work per RESEARCH).
- Plan 06 should:
  1. Verify `chezmoi state dump` on Mac personal contains zero `flameshot` references (defense-in-depth against the research-vs-reality delta documented above).
  2. Reconcile pre-existing `.zshrc` DEBUG=1 drift as planned.
  3. Carry the "grep `chezmoi state dump` for all about-to-be-deleted paths" pattern forward into the canonical clean-check.
- Reversibility evidence (`00.5-04-flameshot-baseline.md`) is in place for Phase 0's role=dev+os=linux+not-wsl re-staging — including the `savePath=/Users/jteague/Screenshots` smell that justified role/OS gating in the first place.

## Self-Check: PASSED

Verified on 2026-05-28T03:11Z (Mac personal):

- FOUND: `.planning/phases/00.5-audit-documentation/00.5-04-SUMMARY.md`
- FOUND: `.planning/phases/00.5-audit-documentation/00.5-04-flameshot-baseline.md`
- FOUND: commit `fd18ccd` (Task 1)
- FOUND: commit `5ebf307` (Task 2)
- FOUND: commit `2dd08a2` (Task 3)
- FOUND: `home/private_dot_config/flameshot/` absent in source tree
- FOUND: `~/.config/flameshot/` absent on Mac personal destination
- FOUND: entryState bucket contains zero `flameshot` references

---
*Phase: 00.5-audit-documentation*
*Completed: 2026-05-27*
