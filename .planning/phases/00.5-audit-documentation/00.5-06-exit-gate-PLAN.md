---
phase: 00.5-audit-documentation
plan: 06
type: execute
wave: 4
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - .planning/phases/00.5-audit-documentation/00.5-state-preview.md
  - .planning/phases/00.5-audit-documentation/00.5-drift-reconciliation.md
  - .planning/phases/00.5-audit-documentation/00.5-exit-gate-report.md
autonomous: false
human_in_loop: true
requirements:
  - AUD-04
must_haves:
  truths:
    - "Mac personal `chezmoi diff -x externals` is empty (zero functional diff)"
    - "Mac personal pre-existing drift (DEBUG=1 in .zshrc) reconciled per fix-source/fix-machine/escalate protocol"
    - "Every drift entry has a documented decision in 00.5-drift-reconciliation.md"
    - "Mac work CRLF-in-index pre-check ran and result is documented (LF-clean OR renormalize-required + decision)"
    - "Mac work `chezmoi diff -x externals` empty (zero functional diff) — HARD exit gate"
    - "Mac work chezmoi state captured and appended to 00.5-state-preview.md"
    - "Cross-machine deltas section of state-preview populated"
    - "Phase 0.5 exit-gate report exists summarizing both Macs as PASS"
  artifacts:
    - path: ".planning/phases/00.5-audit-documentation/00.5-drift-reconciliation.md"
      provides: "Per-entry drift decisions on Mac personal (fix-source / fix-machine / escalate-to-Phase-0)"
    - path: ".planning/phases/00.5-audit-documentation/00.5-state-preview.md"
      provides: "Mac work sections + cross-machine deltas now populated (was scaffold after Plan 01, Mac personal added in Plan 03)"
    - path: ".planning/phases/00.5-audit-documentation/00.5-exit-gate-report.md"
      provides: "Final phase-close report with both-Macs diff status, attestation, and Phase 0 input summary"
  key_links:
    - from: "Mac personal drift reconciliation (00.5-drift-reconciliation.md)"
      to: "source-tree changes OR chezmoi apply destination changes"
      via: "per-entry decision execution"
      pattern: "fix-source|fix-machine|escalate"
    - from: "Mac work chezmoi diff -x externals"
      to: "Phase 0.5 exit gate"
      via: "human-pasted output (empty = PASS)"
      pattern: "exit-gate-report"
---

<objective>
Close Phase 0.5 on the load-bearing exit gate: `chezmoi diff -x externals` empty on BOTH Macs. Per CONTEXT: "Mac work is a daily driver — reachable in the phase window. Do not loosen the gate to fit availability." This plan executes the reconciliation work needed to get there.

Three sub-jobs:
1. Mac personal: reconcile the pre-existing source drift (DEBUG=1 in .zshrc per RESEARCH Pitfall B) per the fix-source/fix-machine/escalate protocol.
2. Mac work: run the CRLF-in-index pre-check that Plan 03 deferred + capture Mac work chezmoi state + populate cross-machine deltas.
3. Both Macs: confirm `chezmoi diff -x externals` empty; produce the exit-gate report.

Most of this is human-in-loop because Mac work cannot be touched from Mac personal. Plans here exist as explicit tasks (per VALIDATION.md mandate) so execute-phase status reflects the human-gated reality.

Output: drift reconciliation doc, populated state-preview Mac work + deltas, exit-gate report — Phase 0.5 closed.
</objective>

<execution_context>
@/Users/jteague/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jteague/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00.5-audit-documentation/00.5-CONTEXT.md
@.planning/phases/00.5-audit-documentation/00.5-RESEARCH.md
@.planning/phases/00.5-audit-documentation/00.5-VALIDATION.md
@.planning/phases/00.5-audit-documentation/00.5-state-preview.md
@.planning/phases/00.5-audit-documentation/00.5-03-SUMMARY.md
@.planning/phases/00.5-audit-documentation/00.5-04-SUMMARY.md
@.planning/phases/00.5-audit-documentation/00.5-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate and reconcile Mac personal drift</name>
  <files>.planning/phases/00.5-audit-documentation/00.5-drift-reconciliation.md</files>
  <action>
On Mac personal, run:
  chezmoi diff -x externals > /tmp/00.5-drift-mac-personal.diff

For EACH file in the diff (RESEARCH expects at minimum: .zshrc DEBUG=1 line; possibly others surfaced by Plan 04 flameshot cleanup OR Plan 05 packages.yaml normalization re-fire effects), document a decision row in .planning/phases/00.5-audit-documentation/00.5-drift-reconciliation.md.

File structure:

  # Mac personal drift reconciliation
  Captured: YYYY-MM-DD HH:MM
  chezmoi version: <version>

  ## Drift entries

  | File | Drift summary | Decision | Action taken | Result |
  |------|---------------|----------|--------------|--------|
  | ~/.zshrc | source-template lacks DEBUG=1 line that appears in rendered output; chezmoi apply would remove the line | fix-source / fix-machine / escalate | <what you did> | <chezmoi diff post-action> |
  | ... | ... | ... | ... | ... |

Decision protocol per RESEARCH Pitfall B:
- fix-source: the line is wanted — add it to home/dot_zshrc.tmpl (commit to source). Use this when the line is permanent and belongs in all dev machines.
- fix-machine: the line is one-off — let `chezmoi apply` revert (no source change). Use when the line was hand-edited on this machine only and isn't worth canonicalizing.
- escalate: the line touches taxonomy/role/personal split — document as Phase 0 input and DO NOT BLOCK Phase 0.5 on it. Document the deferral with explicit justification.

For the DEBUG=1 case specifically (per RESEARCH): this is "for Claude Code to debug ember" — likely a temporary dev aid. Recommend `fix-machine` unless Teague says otherwise (chezmoi apply removes it; if needed again, set it ad-hoc).

Execute each decision row:
- fix-source: edit home/dot_zshrc.tmpl, git add, git commit -m "fix(0.5): add DEBUG=1 to .zshrc template per Mac personal drift reconciliation"
- fix-machine: chezmoi apply (interactively review what's being reverted; confirm)
- escalate: just document; no source/machine change

After ALL drift entries reconciled:
  chezmoi diff -x externals
  # Expect: empty output (PASS) OR only escalated entries (also PASS if escalation justifications are sound)

Record the final post-reconciliation state in the doc.
  </action>
  <verify>
    <automated>[[ -f .planning/phases/00.5-audit-documentation/00.5-drift-reconciliation.md ]] && grep -qE 'fix-source|fix-machine|escalate' .planning/phases/00.5-audit-documentation/00.5-drift-reconciliation.md && bash -c 'OUT=$(chezmoi diff -x externals 2>/dev/null); [[ -z "$OUT" ]] || echo "$OUT" | head -5'</automated>
  </verify>
  <done>Drift reconciliation doc exists, every diff entry has a decision + action + post-action result, final `chezmoi diff -x externals` on Mac personal is empty (or contains only justified escalations).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Mac work CRLF-in-index pre-check (human-run on Mac work)</name>
  <files>(checkpoint — human action required; no files written by Claude in this task)</files>
  <action>
WHAT WAS BUILT (context for the human):
Phase 0.5 needs to confirm Mac work's git index is also LF-clean (or surface CRLF as a separate decision per RESEARCH Pitfall E). This is the pre-check Plan 03 deferred for Mac work.

HOW TO VERIFY (steps for the human to perform):
On Mac work (SSH in OR sit at the machine):

1. cd to the chezmoi source repo (the same .local/share/chezmoi/ path)
2. Run:
     git fetch origin
     git pull --ff-only origin <branch>   # ensure Mac work has the Plan 02-05 commits including .gitattributes
3. Run the CRLF pre-check:
     git ls-files --eol -- 'home/**/*.tmpl' | grep -v 'i/lf'
4. Expected: empty output (Mac work is LF-clean like Mac personal). Paste output here (or "empty").

If non-empty:
  - Output shows i/crlf or i/mixed entries → run `git add --renormalize .` per CONTEXT's "decide explicitly per phase" guidance; verify diff is just line-ending changes; commit as "fix(0.5): renormalize CRLF entries to LF on Mac work index". Re-run pre-check; should now be empty.
  - If renormalize produces unexpected content changes, STOP and surface — there's deeper drift.

Paste the pre-check output (empty or otherwise) + any actions taken into the resume signal.
  </action>
  <verify>
    <automated>MISSING — human-in-loop checkpoint; verification is the resume-signal paste recorded in the next autonomous task. Downstream task will assert on the file(s) the human action produced.</automated>
  </verify>
  <done>Human completed the verify steps above AND provided the resume signal: Paste pre-check output. If non-empty, describe renormalize result. Type "approved" once Mac work is confirmed LF-clean (with or without a renormalize commit).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Mac work chezmoi state capture (human-run on Mac work)</name>
  <files>(checkpoint — human action required; no files written by Claude in this task)</files>
  <action>
WHAT WAS BUILT (context for the human):
Plan 03 populated Mac personal sections of 00.5-state-preview.md. Mac work section is still scaffold.

HOW TO VERIFY (steps for the human to perform):
On Mac work, run and paste output:

1. date "+%Y-%m-%d %H:%M %Z"
2. chezmoi --version
3. chezmoi state get-bucket --bucket=scriptState
4. chezmoi state get-bucket --bucket=entryState
5. chezmoi managed | grep -E 'flameshot|generate-gpg-key' (confirm — or NOT — the same paths Mac personal has)

Paste all outputs here. Planner will edit 00.5-state-preview.md to populate the Mac work section AND compute the Cross-machine deltas section (scripts/entries in state on personal but not work, or vice versa) using your pasted data + the existing Mac personal data.

Specifically watch for:
- Is generate-gpg-key.sh in Mac work scriptState? (Per RESEARCH it shouldn't be — but confirming on Mac work matters for Phase 0.)
- Is ~/.config/flameshot/ in Mac work entryState? (Per AUD-03 it's been removed from source — Plan 04 — and Mac work pulled the commit. After Mac work runs `chezmoi apply`, it should be gone from entryState too.)
- Any scripts in scriptState on Mac work that AREN'T on Mac personal? (work-specific run_onchange firings — Phase 0 input.)
  </action>
  <verify>
    <automated>MISSING — human-in-loop checkpoint; verification is the resume-signal paste recorded in the next autonomous task. Downstream task will assert on the file(s) the human action produced.</automated>
  </verify>
  <done>Human completed the verify steps above AND provided the resume signal: Paste all 5 outputs. Type "captured" when done; planner updates state-preview.md.</done>
</task>

<task type="auto">
  <name>Task 4: Update state-preview.md Mac work + Cross-machine deltas + Mac work exit-gate diff verification</name>
  <files>.planning/phases/00.5-audit-documentation/00.5-state-preview.md, .planning/phases/00.5-audit-documentation/00.5-exit-gate-report.md</files>
  <action>
PART A — Populate Mac work section of state-preview using Teague's pasted output from Task 3. Mirror the format Plan 03 used for Mac personal (Captured timestamp, chezmoi version, scriptState table, entryState table with flameshot/generate-gpg-key callouts).

PART B — Compute Cross-machine deltas section:
- Scripts in scriptState on Mac personal but NOT Mac work (set difference)
- Scripts in scriptState on Mac work but NOT Mac personal (set difference)
- entryState entries differing between machines (especially: any that touch packages.yaml-derived files post-Plan-05 normalization)
- Flag any deltas that Phase 0 must be aware of (e.g., work-specific scripts that need work=true gating in the refactored .chezmoiignore)

PART C — Issue the Mac work exit-gate request:
Add to .planning/phases/00.5-audit-documentation/00.5-exit-gate-report.md a section "## Mac work exit gate" with a placeholder for Teague's pasted output. Task 5 collects it.

PART D — Write the Mac personal portion of the exit-gate report:
  # Phase 0.5 Exit Gate Report
  Closed: YYYY-MM-DD

  ## Mac personal
  - chezmoi diff -x externals: EMPTY ✓ (or list escalations with justifications)
  - Drift reconciliation: see 00.5-drift-reconciliation.md
  - AUD-01..05 + SS-01 verifications (run quick.sh --strict; paste summary)

  ## Mac work
  (Task 5 populates after Teague runs the exit-gate diff on Mac work)

  ## Phase 0 input summary
  - state-preview: .planning/phases/00.5-audit-documentation/00.5-state-preview.md
  - candidate decisions: .planning/phases/00.5-audit-documentation/00.5-packages-candidates.md
  - drift escalations (if any): from 00.5-drift-reconciliation.md
  - docs flagged inconsistencies (if any): from 00.5-02-SUMMARY.md
  - flameshot baseline (recoverable .ini content): .planning/phases/00.5-audit-documentation/00.5-04-flameshot-baseline.md
  </action>
  <verify>
    <automated>grep -q 'Mac work' .planning/phases/00.5-audit-documentation/00.5-state-preview.md && grep -qE 'Captured.*[0-9]{4}-[0-9]{2}-[0-9]{2}' .planning/phases/00.5-audit-documentation/00.5-state-preview.md && grep -q 'Cross-machine deltas' .planning/phases/00.5-audit-documentation/00.5-state-preview.md && [[ -f .planning/phases/00.5-audit-documentation/00.5-exit-gate-report.md ]] && grep -q 'Mac personal' .planning/phases/00.5-audit-documentation/00.5-exit-gate-report.md && bash -c 'OUT=$(chezmoi diff -x externals 2>/dev/null); [[ -z "$OUT" ]]'</automated>
  </verify>
  <done>state-preview Mac work section + Cross-machine deltas populated from Teague's data. exit-gate-report exists with Mac personal section filled. chezmoi diff -x externals on Mac personal is empty.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Mac work exit-gate diff (HARD gate — human-run on Mac work)</name>
  <files>(checkpoint — human action required; no files written by Claude in this task)</files>
  <action>
WHAT WAS BUILT (context for the human):
Mac personal exit gate verified empty (Task 4). Now Mac work — the hard exit gate per CONTEXT.

HOW TO VERIFY (steps for the human to perform):
On Mac work (with all Phase 0.5 commits pulled including Plans 02-05 + any source changes from Plan 06 Task 1):

1. cd to the chezmoi source repo
2. git pull --ff-only — ensure you have all phase commits
3. chezmoi diff -x externals
4. Expected: empty output. PASS.

If non-empty:
- Externals churn (.oh-my-zsh, tpm) → false alarm (Pitfall A). You're running `-x externals` so this shouldn't appear; if it does, double-check the flag.
- Real source drift on Mac work → apply the SAME drift reconciliation protocol Task 1 used for Mac personal: fix-source / fix-machine / escalate. Document each entry. Re-run the diff. Repeat until empty.

Paste the FINAL output (empty, OR justified escalations only). Per CONTEXT: "If Mac work diff is dirty post-audit, fix the source (or the machine) before closing the phase." Phase does NOT close until Mac work diff is empty (or escalations are explicit and accepted as Phase 0 input).

Optional but recommended: also run `bash .planning/phases/00.5-audit-documentation/checks/full.sh` on Mac work for completeness — every section should PASS in strict mode.

This is the load-bearing gate. Phase 0's merge gate is the same diff on the same two Macs.
  </action>
  <verify>
    <automated>MISSING — human-in-loop checkpoint; verification is the resume-signal paste recorded in the next autonomous task. Downstream task will assert on the file(s) the human action produced.</automated>
  </verify>
  <done>Human completed the verify steps above AND provided the resume signal: Paste FINAL chezmoi diff -x externals output from Mac work (empty or escalations). Type "GATE PASS" to close Phase 0.5.</done>
</task>

<task type="auto">
  <name>Task 6: Finalize exit-gate report + commit</name>
  <files>.planning/phases/00.5-audit-documentation/00.5-exit-gate-report.md</files>
  <action>
Append Mac work section to 00.5-exit-gate-report.md using Teague's Task 5 paste:
  ## Mac work
  - Captured: YYYY-MM-DD HH:MM
  - chezmoi version: <from Task 3 capture>
  - chezmoi diff -x externals: EMPTY ✓ (or list escalations)
  - CRLF pre-check (Task 2): LF-clean ✓ (or "renormalize committed in <SHA>")
  - full.sh on Mac work (if Teague ran it): PASS / NOT RUN

Add the attestation block:

  ## Phase 0.5 Attestation
  Closed: YYYY-MM-DD
  All AUD-01..05 + SS-01 requirements verified.
  Both-Mac exit gate: PASS (Mac personal: <SHA at close>; Mac work: <SHA at close>)
  Phase 0 inputs: documented in "Phase 0 input summary" section above.

  ### Anti-scope-creep verification
  - [ ] No taxonomy/role/personal axis changes (Phase 0 territory) ✓
  - [ ] generate-gpg-key.sh NOT deleted (Phase 0 owns) ✓
  - [ ] No .chezmoiignore templating (Phase 0 owns) ✓
  - [ ] No new promptStringOnce prompts (Phase 0 owns) ✓
  - [ ] No VaultWarden/secret-plane work (Phase 1 owns) ✓

Commit:
  git add .planning/phases/00.5-audit-documentation/00.5-exit-gate-report.md .planning/phases/00.5-audit-documentation/00.5-state-preview.md .planning/phases/00.5-audit-documentation/00.5-drift-reconciliation.md
  git commit -m "Phase 0.5 close: exit gate PASS on both Macs"
  </action>
  <verify>
    <automated>grep -q 'GATE PASS\|EMPTY ✓\|exit gate: PASS\|Phase 0.5 Attestation' .planning/phases/00.5-audit-documentation/00.5-exit-gate-report.md && grep -q 'Mac personal' .planning/phases/00.5-audit-documentation/00.5-exit-gate-report.md && grep -q 'Mac work' .planning/phases/00.5-audit-documentation/00.5-exit-gate-report.md && git log -1 --pretty=format:%s | grep -q '0.5'</automated>
  </verify>
  <done>Exit-gate report complete with both-Mac sections, attestation block, anti-scope-creep checklist. Commit landed marking phase close.</done>
</task>

</tasks>

<verification>
After all tasks:
- bash .planning/phases/00.5-audit-documentation/checks/full.sh (FULL strict mode, no flags) — every section PASS on Mac personal
- Mac work (per Teague's Task 5 paste): chezmoi diff -x externals empty
- All 6 requirements (AUD-01..05 + SS-01) verified across the candidate list, docs, .gitattributes, flameshot removal, packages.yaml, and SS-01 inspection
- Anti-scope-creep checklist all green
</verification>

<success_criteria>
- Mac personal chezmoi diff -x externals: empty (or only justified escalations)
- Mac work chezmoi diff -x externals: empty (or only justified escalations) — paste-confirmed
- 00.5-drift-reconciliation.md complete (Mac personal)
- 00.5-state-preview.md complete (both Macs + deltas)
- 00.5-exit-gate-report.md complete with attestation
- full.sh in strict mode PASSes on Mac personal
- Phase 0 has all inputs it needs (state preview, candidate decisions, drift escalations, docs inconsistencies)
- Anti-scope-creep checklist verified
</success_criteria>

<output>
Create .planning/phases/00.5-audit-documentation/00.5-06-SUMMARY.md AND .planning/phases/00.5-audit-documentation/00.5-SUMMARY.md (phase-level) documenting:
- Both-Mac gate PASS status
- All 6 requirement IDs verified
- Pointer to exit-gate-report for Phase 0 to consume
- STATE.md update: mark Phase 0.5 complete, set next focus to Phase 0
</output>
