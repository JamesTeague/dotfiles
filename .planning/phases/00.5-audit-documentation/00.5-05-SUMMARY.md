---
phase: 00.5-audit-documentation
plan: 05
subsystem: packages
tags: [packages-yaml, brew-bundle, AUD-01, AUD-02, normalization, dead-code, naming-drift, mac-personal]

requires:
  - phase: 00.5-audit-documentation
    provides: "Wave 0 harness (quick.sh AUD-01/02 section + full.sh step 5 approved-snapshot diff); candidate-removal scaffold (00.5-packages-candidates.md); RESEARCH §AUD-02 dead-path analysis (personal.casks + work.core); CONTEXT decisions (AUD-02 LIGHT scope; AUD-01 not autonomous — Teague review gate)"
provides:
  - "home/.chezmoidata/packages.yaml LIGHT-normalized: personal.casks → personal.darwin.casks (4 items), work.core block fully eliminated (items moved to work.darwin.* OR deleted per Teague's review), 3 core-brews naming-drift renames (python3→python@3.14, npm→node, gpg→gnupg), 1 work-brew rename (localstack-cli→localstack)"
  - ".planning/phases/00.5-audit-documentation/checks/approved-brew-bundle.txt — byte-exact snapshot of `chezmoi execute-template < run_onchange_before_02-install-packages.sh.tmpl`. Future renders are diffed against this by full.sh step 5. SHA256 18007775519fe0a6ac4be97e14f5067e02c55a8e876e99d7eb1923d5ca565a00 (2117 bytes; 129 lines)."
  - ".planning/phases/00.5-audit-documentation/00.5-packages-candidates.md — Final Commit Manifest populated (derived via decision-merge fallback rule)."
  - "Empirical finding (research delta): chezmoi apply --dry-run successfully shows 02-install-packages.sh pending re-fire with NEW SHA-prefix 1758801412... (was f-prefix before the rename/move). Pitfall D (run_onchange re-fire) confirmed expected behavior. brew bundle will be no-op on Mac personal because all three renamed brews (python@3.14, node, gnupg) are ALREADY installed under their canonical formula names."
affects:
  - "00.5-06-exit-gate (AUD-01 + AUD-02 rows flip to PASS; full.sh step 5 now PASS; quick.sh stays at 12 PASS / 0 PENDING / 0 FAIL; Plan 06 still owns .zshrc DEBUG=1 reconciliation + Mac work parity)"
  - "phase-0-refactor TAX-05 (packages.yaml axis restructure starts from a state with: work.core eliminated, personal.casks emptied of moved items but block still present, all naming drift resolved upstream — so the role-axis rewrite begins from canonical formula names and a normalized personal/work × {darwin,linux} skeleton)"
  - "phase-0-refactor TAX-05 inherits an OPEN ITEM: removal of empty-placeholder blocks (linux.taps, linux.casks, personal.linux.*, work.linux.*, plus personal.taps, personal.brews) requires brew template `range` guard rewrite via `hasKey` — left as defer per AUD-02 LIGHT scope."

tech-stack:
  added: []
  patterns:
    - "Decision-merge fallback pattern for human-verify candidate review: Decision column overrides Recommendation; blank Decision falls back to Recommendation verbatim. Captured in candidate file + commit body so the derivation is auditable. Avoids re-prompting Teague when defaults are already correct."
    - "RENAME-treated-as-in-place-edit pattern: when an upstream formula renames (python3→python@3.14, gpg→gnupg, npm→node), edit the entry in place rather than DELETE+ADD. Preserves comments + array ordering + diff hygiene. Distinguish from MOVE (which changes block) — both can apply together (localstack-cli was BOTH renamed AND moved)."
    - "Snapshot-byte-equal verification pattern: full.sh step 5 diffs `chezmoi execute-template` output against the committed approved-brew-bundle.txt snapshot. Catches accidental packages.yaml drift before it ships. Snapshot regenerates only when packages.yaml legitimately changes (next time: Phase 0 TAX-05)."
    - "Hand-edit-over-yaml-roundtrip pattern: AUD-02 LIGHT scope (small, surgical mutations) used targeted Edit tool calls instead of PyYAML safe_dump round-trip. Preserved single-quoted-string style + key ordering + inline comments verbatim. Diff was structurally minimal."

key-files:
  created:
    - ".planning/phases/00.5-audit-documentation/checks/approved-brew-bundle.txt — rendered brew bundle snapshot (129 lines, 2117 bytes); reference for full.sh step 5 diff"
    - ".planning/phases/00.5-audit-documentation/00.5-05-SUMMARY.md — this file"
  modified:
    - "home/.chezmoidata/packages.yaml — 184 insertions, 58 deletions (per git --stat); 3 renames in core.brews; personal.casks emptied (items moved to personal.darwin.casks); work.core block removed entirely (items moved to work.darwin.* OR deleted); inline comments preserved + added"
    - ".planning/phases/00.5-audit-documentation/00.5-packages-candidates.md — Final Commit Manifest populated (decision-merge fallback applied)"

key-decisions:
  - "Decision-merge fallback rule applied per Teague's resume signal: 'approved' with explicit instruction that any blank Decision cell falls back to Recommendation verbatim. Derived manifest captured in candidates.md Final Commit Manifest section AND echoed into commit body for traceability. No re-prompting needed for the ~60 keep rows."
  - "Three core-brews renamed in place (not deleted) to track upstream rename: python3 → python@3.14, npm → node, gpg → gnupg. All three canonical names are ALREADY installed on Mac personal under those exact formula names — brew bundle re-fire is no-op. The original `npm` and `node`-via-work.core.brews entries collapsed to a SINGLE `node` entry in packages.core.brews (deduplication side effect of the npm rename)."
  - "synk (typo) and Teague-flagged unwanted items (FinalCutPro mas, postman cask, node from work.core) were DELETE'd rather than moved — Teague used the Decision column to override the original `move` Recommendation. This is exactly what the human-review gate exists to catch (autonomous logic would have moved them since 'not installed' on Mac personal isn't sufficient to delete a work item without Teague's knowledge of Mac work install status)."
  - "Empty placeholder blocks (linux.taps, linux.casks, personal.linux.*, work.linux.*, personal.taps, personal.brews) all KEPT per AUD-02 LIGHT scope discipline. Removal requires brew template `range` guard rewrite via `hasKey` — explicitly DEFERRED to Phase 0 TAX-05 with documented rationale."
  - "Hand-edit over PyYAML round-trip — packages.yaml uses specific stylistic conventions (single-quoted strings, inline comments, particular key ordering matching the schema). PyYAML safe_dump would normalize all of these and produce a noisy diff. Used targeted Edit tool calls instead; diff is structurally minimal and reviewable."
  - "Did NOT run `chezmoi apply` (per critical_constraint). `chezmoi apply --dry-run --verbose` confirmed 02-install-packages.sh pending re-fire (new hash) as expected per Pitfall D. .zshrc:80 DEBUG=1 line untouched — Plan 06 territory preserved."

patterns-established:
  - "Decision-merge fallback (Decision overrides; blank → Recommendation) is the canonical resume protocol when a human-verify checkpoint returns 'approved' without per-row decisions. Cite this plan when the pattern recurs in future audit-style work."
  - "Hand-edit YAML when style preservation matters more than mechanical correctness. PyYAML/ruamel round-trip is the right call for bulk transformations; targeted Edit is the right call for LIGHT scopes (under ~10 edits)."

requirements-completed: [AUD-01, AUD-02]

duration: 45 min
completed: 2026-05-28
---

# Phase 00.5 Plan 05: Packages Audit Summary

**packages.yaml LIGHT-normalized in one atomic commit: 3 naming-drift renames (python3→python@3.14, npm→node, gpg→gnupg) in core.brews + work.core block fully eliminated (12 items moved to work.darwin.* + 4 items deleted per Teague's review) + personal.casks moved to personal.darwin.casks (4 items) + approved-brew-bundle snapshot captured for full.sh step 5 byte-exact verification — AUD-01 and AUD-02 both closed without touching pre-existing .zshrc DEBUG=1 drift (Plan 06 territory preserved).**

## Performance

- **Duration:** ~45 min (spanned a checkpoint pause for Teague review; Task 1 commit on 2026-05-28T02:43Z, Task 3 commit on 2026-05-28T03:34Z)
- **Tasks:** 3 (2 auto + 1 human-verify checkpoint)
- **Files modified:** 3 (packages.yaml + candidates.md + approved-brew-bundle.txt)

## Accomplishments

- AUD-01 requirement complete — dead `packages.yaml` entries pruned (synk typo, unwanted Final Cut Pro mas, unwanted postman cask, duplicate work.core node) via Teague-reviewed manifest
- AUD-02 requirement complete (LIGHT scope) — `work.core` block fully eliminated; `personal.casks` dead-path emptied; both blocks of inconsistent nesting normalized to consume the live brew template paths
- 3 upstream-naming-drift renames resolved (python3→python@3.14, npm→node, gpg→gnupg) — brew bundle now references canonical formula names that match what's actually installed
- 1 misnamed work-brew renamed in flight (localstack-cli → localstack)
- approved-brew-bundle.txt snapshot captured at SHA256 `18007775...565a00` — full.sh step 5 now PASS (byte-exact diff)
- Wave 0 quick.sh remains at 12 PASS / 0 PENDING / 0 FAIL
- Plan 06's `.zshrc` reconciliation territory preserved — `~/.zshrc:80` `DEBUG=1` line confirmed untouched

## Final Commit Manifest (executed)

**DELETE (4 items):**
- `packages.personal.darwin.mas: FinalCutPro` — Teague: stale "10.6.5" label, no longer wanted
- `packages.work.core.brews: synk` — typo entry (no such formula upstream); delete not rename
- `packages.work.core.brews: node` — dedupe (superseded by `npm` → `node` rename in core.brews)
- `packages.work.core.casks: postman` — Teague: no longer wanted

**MOVE (12 items across 5 group ops):**
- `packages.personal.casks → packages.personal.darwin.casks`: mullvadvpn, kindle, handbrake, rode-central (AUD-02 LIGHT — items installed on Mac personal but dead-path in current YAML)
- `packages.work.core.taps → packages.work.darwin.taps`: snyk/tap, localstack/tap
- `packages.work.core.brews → packages.work.darwin.brews`: awscli, stern, sqlc, git-lfs, dotnet@8
- `packages.work.core.brews → packages.work.darwin.brews` (with rename): localstack-cli → localstack
- `packages.work.core.casks → packages.work.darwin.casks`: microsoft-teams

**RENAME (3 in-place edits, packages.core.brews):**
- python3 → python@3.14 (upstream renamed; `python3` is alias only)
- npm → node (npm ships with node; no standalone formula; also displaces the deleted work.core node)
- gpg → gnupg (gpg is alias only)

**POST-CLEANUP:**
- `packages.work.core` block removed entirely (all subkeys emptied by moves/deletes)
- `packages.personal.{taps,brews}` left as empty placeholders (removal requires brew template guard rewrite — Phase 0 TAX-05 territory)

**KEEP (61 rows):**
- All packages.core.* survivors (44 brews + 14 casks + 1 tap)
- All packages.darwin.* (2 taps + 4 brews + 5 casks)
- All packages.linux.* (2 brews + empty placeholders)
- All packages.personal.darwin.* survivors (1 brew + 3 casks + Brother iPrint mas)
- All packages.work.darwin.* empty placeholders that now receive moves
- All packages.work.linux.* empty placeholders

**DEFER (Phase 0 / TAX-05):**
- Removal of empty placeholder blocks (linux.taps, linux.casks, personal.linux.*, work.linux.*) — requires brew template `range` guarding via `hasKey`
- Any axis restructure (current personal/work × {core,darwin,linux} → role-based) — Phase 0 territory

## Verification Outcomes

- **YAML structural validity:** PASS (`ruby -ryaml -e "YAML.load_file('home/.chezmoidata/packages.yaml')"` exits 0; top-level keys confirmed: core, darwin, linux, personal, work; personal subkeys: taps, brews, casks, darwin, linux; work subkeys: darwin, linux — note `work.core` absent as designed)
- **chezmoi execute-template render:** PASS — 129 lines / 52 brew / 3 tap / 26 cask
- **Snapshot byte-exact diff:** PASS — full.sh step 5 confirms `chezmoi execute-template < run_onchange_before_02-install-packages.sh.tmpl` matches approved-brew-bundle.txt byte-for-byte
- **Renamed brews installed on Mac personal:** CONFIRMED — `brew list --formula | grep -E '^(python@3.14|node|gnupg)$'` returns all three; brew bundle re-fire will be idempotent no-op
- **chezmoi apply --dry-run (Pitfall D check):** Confirmed `02-install-packages.sh` pending re-fire with NEW hash prefix `1758801412...` — expected per Pitfall D; idempotent because all referenced brews/casks are already installed. (Dry-run exit=1 because of `.zshrc:80` DEBUG=1 TTY prompt — pre-existing drift owned by Plan 06; NOT introduced by Plan 05.)
- **.zshrc DEBUG=1 untouched:** CONFIRMED — Plan 06 territory preserved.

## Decisions Made

1. **Decision-merge fallback (Decision overrides Recommendation; blank → Recommendation verbatim)** — Teague's resume signal "approved" + explicit fallback rule authorized derived manifest construction without re-prompting on the ~60 keep rows. Rule + derived manifest committed to candidates.md AND echoed in commit body for auditability.

2. **Hand-edit packages.yaml over PyYAML/ruamel round-trip** — LIGHT scope (under ~15 mutations) + style preservation (single-quoted strings, inline comments, key ordering) made targeted Edit tool calls the right tradeoff. Diff is structurally minimal; reviewer can read intent at a glance.

3. **Renames-not-deletes for upstream formula drift** — python3, npm, gpg renamed in place rather than DELETE+ADD. Preserved inline comments (`# required by lsp tsserver` etc.) + array ordering. The npm → node rename also displaces the duplicate `node` from work.core.brews (which was DELETE'd per Teague), so the final state has exactly one canonical `node` entry across the whole file.

4. **DELETE overrides for synk, FinalCutPro, postman, and work.core node** — Teague used the Decision column to override `move` Recommendations. This is the exact case the human-review gate exists to catch: items that are "not installed on Mac personal" could reasonably be moved (for Mac work activation) OR deleted (no longer wanted) — only Teague has the cross-machine context to decide.

5. **Did NOT run `chezmoi apply`** — per critical_constraint. Dry-run verification only. `.zshrc` DEBUG=1 reconciliation belongs to Plan 06.

## Deviations from Plan

### Auto-fixed Issues

None. The plan executed exactly as written, with one exception handled per Teague's explicit fallback rule:

**1. [Rule 4 deferred to user — handled via decision-merge fallback at checkpoint, not auto-fix] Decision column was not exhaustively filled by Teague**

- **Found during:** Task 3 (resume from checkpoint)
- **Situation:** Plan 05 Task 2 expected Teague to fill the Decision column for all ~79 rows. Teague filled only the rows requiring explicit override (~11 rows) and authorized "for any row where Decision is blank, fall back to the Recommendation column verbatim."
- **Resolution:** Applied the fallback rule to construct the effective Final Commit Manifest. Captured the derivation explicitly in candidates.md (Final Commit Manifest section now documents that it was computed via fallback) AND in the commit body. This makes the derivation auditable and reproducible.
- **Why this is not an auto-fix:** Teague's resume signal explicitly authorized this protocol. It's the gated decision the checkpoint existed to surface, with a documented derivation rule rather than per-row enumeration. Captured here for pattern recognition by future plans.

**Total deviations:** 1 (handled via explicit checkpoint protocol — NOT an auto-fix)
**Impact on plan:** Zero scope change. Execution matched plan intent; mechanism was streamlined per Teague's authorization.

## Research vs Reality Delta

The plan anticipated three risk areas that materialized as expected:

| Predicted (RESEARCH / Pitfall D) | Observed (Mac personal, chezmoi 2.70.4)     | Delta                                                      |
| -------------------------------- | ------------------------------------------- | ---------------------------------------------------------- |
| run_onchange_02-install-packages.sh will re-fire after packages.yaml change | CONFIRMED — `chezmoi apply --dry-run` shows pending re-fire with new hash prefix `1758801412...` | Pitfall D mitigated by approved-snapshot snapshot ahead of run; brew bundle is no-op (all installed) |
| PyYAML may re-quote / re-order keys, producing noisy diff | Avoided by hand-edit approach | Decision driven by AUD-02 LIGHT scope; pattern documented for future LIGHT vs HEAVY scope choices |
| Naming-drift formulae may already be installed under canonical names | CONFIRMED for all three (python@3.14, node, gnupg already present via `brew list --formula`) | Renames are pure metadata fix — zero install side-effects on Mac personal |

**New finding:** `chezmoi apply --dry-run --verbose` exits NONZERO (=1) when it encounters interactive prompts in pre-existing source files (the `.zshrc:80` DEBUG=1 prompt). This is a discoverability issue for any dry-run-based verification flow that doesn't account for Plan 06 drift. Plan 06's exit-gate should consider: (a) running dry-run AFTER reconciling .zshrc, OR (b) filtering the dry-run for the specific paths under audit (rather than exit-code gating).

## Issues Encountered

None during execution. The pre-existing working-tree drift (`.planning/config.json`, two nvim plugin files) noted at commit time was explicitly NOT staged — kept Plan 05 surgical, matching Plan 04's pattern.

## Authentication Gates

None.

## Deferred Issues

None — all in-scope work completed.

**Carried forward to Phase 0 / TAX-05:**
- Empty placeholder block removal (requires brew template `range` guard rewrite via `hasKey`)
- Axis restructure (personal/work × {darwin,linux} → role-based)
- Re-snapshot approved-brew-bundle.txt after TAX-05 restructure (current snapshot is the canonical pre-TAX-05 baseline)

## Next Phase Readiness

- AUD-01 + AUD-02 both PASS on Mac personal. Mac work is unverified for `work.darwin.*` install completeness (Plan 06 territory; the moves are designed to be idempotent on next `chezmoi apply` on Mac work — `brew bundle` installs missing items).
- Plan 06 (exit-gate) is now unblocked for AUD-01 + AUD-02 on Mac personal.
- Plan 06 should additionally:
  1. Verify Mac work `brew bundle` against the moved items (`work.darwin.taps/brews/casks` should produce installs if any are missing on Mac work; user-initiated review of new installs).
  2. Reconcile pre-existing `.zshrc` DEBUG=1 drift (Pitfall B in RESEARCH; still outstanding).
  3. Consider adopting the dry-run-exit-code finding (above) into the canonical clean-check pattern.

## Self-Check: PASSED

Verified on 2026-05-28T03:35Z (Mac personal):

- FOUND: `.planning/phases/00.5-audit-documentation/00.5-05-SUMMARY.md`
- FOUND: `.planning/phases/00.5-audit-documentation/checks/approved-brew-bundle.txt` (SHA256 18007775519fe0a6ac4be97e14f5067e02c55a8e876e99d7eb1923d5ca565a00)
- FOUND: commit `bd8148b` (Task 1 — candidate list populated)
- FOUND: commit `b8094e6` (Task 3 — AUD-01 + AUD-02 prune + LIGHT normalize + snapshot)
- FOUND: home/.chezmoidata/packages.yaml structurally valid (ruby YAML.load_file passes)
- FOUND: `work.core` block absent in packages.yaml (correctly removed)
- FOUND: `personal.darwin.casks` contains all 4 moved items (mullvadvpn, kindle, handbrake, rode-central)
- FOUND: `work.darwin.brews` contains all 6 moved items (awscli, localstack, stern, sqlc, git-lfs, dotnet@8)
- FOUND: `packages.core.brews` contains canonical renamed forms (python@3.14, node, gnupg; python3/npm/gpg absent)
- FOUND: quick.sh reports 12 PASS / 0 PENDING / 0 FAIL
- FOUND: full.sh --no-diff-gate --no-quick reports 3 PASS / 0 PENDING / 0 FAIL (step 5 snapshot diff PASS)

---
*Phase: 00.5-audit-documentation*
*Completed: 2026-05-28*
