---
phase: 00.5-audit-documentation
plan: 03
subsystem: infra
tags: [gitattributes, line-endings, chezmoi-state, scriptState, entryState, preview]

requires:
  - phase: 00.5-audit-documentation
    provides: "Wave 0 harness (quick.sh AUD-04 section) + state-preview scaffold (Plan 01)"
provides:
  - "Repo-root .gitattributes enforcing LF for *.tmpl, *.sh, *.ps1 + baseline * text=auto + binary opt-outs"
  - "Mac personal scriptState capture (29 entries / 6 scripts) populated into 00.5-state-preview.md"
  - "Mac personal entryState summary (1673 entries total, key Phase 0 reshape targets itemized)"
  - "Empirical confirmation of two RESEARCH surprises: generate-gpg-key.sh NOT in scriptState; externals refreshPeriod=168h confirmed"
affects:
  - "00.5-06-exit-gate (Mac work CRLF pre-check + Mac work state capture remain human-in-loop)"
  - "phase-0-refactor (consumes state-preview to plan rename-vs-state-delete decisions)"
  - "phase-2-windows (consumes .gitattributes LF policy before Windows-side work begins)"

tech-stack:
  added: []
  patterns:
    - "Repo-root .gitattributes (git-side concern, separate from chezmoi:template:line-ending=native render-time directive)"
    - "State-preview as Phase 0 input artifact — query once, document key reshape targets inline, leave full-bucket re-query to consumer"

key-files:
  created:
    - ".gitattributes — 17 lines; LF policy for *.tmpl/*.sh/*.ps1 + binary types"
  modified:
    - ".planning/phases/00.5-audit-documentation/00.5-state-preview.md — Mac personal section populated (scriptState table of 29 entries + entryState itemization of 9 Phase 0 reshape targets); Surprises section annotated with two ✓ Verified on Mac personal markers; Mac work + Cross-machine deltas preserved as scaffold"

key-decisions:
  - "Skip `git add --renormalize` — Mac personal pre-check showed 0 CRLF entries in index across all 12 .tmpl files; renormalize would be no-op. Documented in Task 1 commit message; will be captured in docs/conventions.md (Plan 02)."
  - "Adopt the 'empirically safe baseline' from RESEARCH verbatim for .gitattributes content — every line validated against current tree shape; includes *.png/*.jpg/*.ico/*.streamDeckProfile binary opt-outs beyond the required *.tmpl rule."
  - "Itemize only Phase-0-reshape entryState entries (9 entries) rather than enumerating all 1673 — full enumeration would be noise; Phase 0 can re-query bucket directly for anything else."
  - "Annotate both RESEARCH Surprises with explicit '✓ Verified on Mac personal' markers inline (rather than as a separate verification section) so future readers see the empirical confirmation co-located with the claim."

patterns-established:
  - "Pre-check before .gitattributes commit: git ls-files --eol -- '<scope>' | grep -v 'i/lf' must be empty before writing the file; skip-renormalize decision recorded in commit message"
  - "State-preview population pattern: capture timestamp + chezmoi version + bucket counts + itemized reshape-target table per machine; preserve other-machine sections as scaffold"

requirements-completed: [AUD-04]

duration: 3 min
completed: 2026-05-27
---

# Phase 00.5 Plan 03: gitattributes + state-peek Summary

**Repo-root `.gitattributes` enforcing LF for `*.tmpl`/`*.sh`/`*.ps1` lands with zero index churn (Mac personal pre-check empirically clean) AND Mac personal `scriptState` (29 entries / 6 scripts) + key Phase-0-reshape `entryState` entries captured into `00.5-state-preview.md` — verifying both RESEARCH surprises (generate-gpg-key.sh NOT a `run_once_`; externals refresh weekly).**

## Performance

- **Duration:** 3 min
- **Started:** 2026-05-27T22:56:08Z
- **Completed:** 2026-05-27T22:59:45Z
- **Tasks:** 2
- **Files modified:** 2 (1 created — `.gitattributes`; 1 modified — `00.5-state-preview.md`)

## Accomplishments

- Repo-root `.gitattributes` lands with zero index churn — Mac personal pre-check confirmed all 12 `.tmpl` files are `i/lf w/lf`, `git add .gitattributes` produced only the new-file entry in `git diff --cached --stat`. AUD-04 section of `quick.sh` now PASS (was PENDING).
- Mac personal `scriptState` fully captured: 29 hash entries across 6 distinct scripts. `02-install-packages.sh` dominates (22 entries — expected for `run_onchange_` with mutating `packages.yaml`). Sorted-by-runAt table preserved as machine-readable Phase 0 input.
- Mac personal `entryState` itemized for the 9 entries Phase 0 will reshape (flameshot dir + ini, generate-gpg-key.sh, the 6 chezmoi-side script entries). Total bucket size (1673 entries, 431/1215/19/6/2 by type) documented; full enumeration deliberately skipped.
- Both RESEARCH surprises empirically verified on Mac personal: `generate-gpg-key.sh` is `type: file` in entryState with ZERO scriptState references (confirming Phase 0's re-fire concern doesn't apply); externals `refreshPeriod = "168h"` confirmed in `.chezmoiexternal.toml`. Inline `✓ Verified on Mac personal` markers added to both surprises.
- Phase 0 implications documented: `02-install-packages.sh` will add a 23rd scriptState entry when Plan 05's `packages.yaml` normalization runs (expected, Pitfall D); `.chezmoiscripts/` renames will require `chezmoi state delete --bucket=scriptState` companion steps OR accepting historical orphans; the flameshot parent directory needs explicit `rm -rf` because chezmoi doesn't auto-prune empty dirs.
- `chezmoi diff -x externals` sanity check: unchanged from baseline (only the pre-existing `.zshrc` DEBUG=1 drift documented in Plan 01) — confirming `.gitattributes` has zero chezmoi-side effect, as RESEARCH predicted.

## Task Commits

Each task was committed atomically:

1. **Task 1: Mac personal CRLF pre-check + .gitattributes write** — `0bd5499` (feat)
2. **Task 2: Capture Mac personal chezmoi state into state-preview.md** — `9773856` (feat)

**Plan metadata:** _(appended after this SUMMARY is written)_

## Files Created/Modified

- `.gitattributes` (created, 17 lines) — `* text=auto` baseline + `*.tmpl text eol=lf` + `*.sh text eol=lf` + `*.ps1 text eol=lf` + `*.png/*.jpg/*.ico/*.streamDeckProfile binary` opt-outs.
- `.planning/phases/00.5-audit-documentation/00.5-state-preview.md` (modified, +114/-15 lines) — Mac personal `Captured` + `chezmoi version` + counts populated; full 29-row scriptState table; distribution-by-script table; 9-row entryState itemization for Phase-0-reshape targets; Phase 0 implications subsections (scriptState + entryState); two `✓ Verified on Mac personal` annotations in Surprises section. Status line updated; Mac work + Cross-machine deltas sections preserved verbatim as scaffold.

## Decisions Made

- **Skip `git add --renormalize`.** Mac personal pre-check showed `git ls-files --eol -- 'home/**/*.tmpl' | grep -v 'i/lf'` returns empty across all 12 tracked `.tmpl` files. Per RESEARCH, gitattributes docs: `* text=auto` does not renormalize files already stored as LF. Renormalize would be a no-op. Documented in Task 1 commit message; canonical policy statement deferred to `docs/conventions.md` (Plan 02).
- **Adopt the RESEARCH 'empirically safe baseline' verbatim** for `.gitattributes` content (`* text=auto` + `*.tmpl`/`*.sh`/`*.ps1` + binary opt-outs for png/jpg/ico/streamDeckProfile). Every line was validated against the current tree shape during RESEARCH; no scope creep beyond what was vetted.
- **Itemize only the 9 entries Phase 0 will reshape** in the entryState table rather than enumerating all 1673. Full enumeration is noise — Phase 0 can re-query the bucket directly for anything specific. The 9 chosen entries (flameshot dir + ini, generate-gpg-key.sh, 6 chezmoi-side script entries) cover the AUD-03 target, the generate-gpg-key Phase-0 target, and every `.chezmoiscripts/` reorganization touchpoint.
- **Annotate Surprises inline** with `✓ Verified on Mac personal (Plan 03, 2026-05-27)` markers immediately after each claim, rather than a separate verification section. This keeps the empirical confirmation co-located with the assertion so future readers don't need to cross-reference.

## Deviations from Plan

None - plan executed exactly as written.

The plan's pre-check protocol (Sub-step A) explicitly accounted for the empirically-verified-LF state described in RESEARCH; the pre-check passed on first try; the skip-renormalize decision was pre-approved in the plan's commit-message template; Sub-step C (post-add verification) confirmed zero index churn on first attempt. No Rule 1/2/3 auto-fixes were needed. No Rule 4 (architectural) decisions arose.

---

**Total deviations:** 0
**Impact on plan:** None — plan was thoroughly grounded in RESEARCH empirical findings, leaving no surprises during execution.

## Issues Encountered

- **Pre-existing working-tree changes carried forward from before Plan 01:** `.planning/config.json`, `home/private_dot_config/nvim/lua/plugins/harpoon.lua`, `home/private_dot_config/nvim/lua/plugins/llm.lua`. Documented in Plan 01 SUMMARY as out-of-scope; this plan did NOT touch them; they remain unstaged after this plan. Not a blocker.
- **`docs/` directory appeared mid-plan as `?? docs/` (untracked) with `docs/conventions.md` inside.** This is Plan 02 (Wave 1 sibling, running in parallel) writing its artifact. Not a conflict — Plan 03 doesn't touch `docs/`. Plan 02 will commit it independently.

## User Setup Required

None — no external services configured by this plan.

## Next Phase Readiness

- **AUD-04 complete on Mac personal side.** Mac work side (CRLF-in-index pre-check + `.gitattributes` validation) is queued for Plan 06 as a human-in-loop task per Pitfall E. The Mac work pre-check might still fail (separate clone, potentially different `core.autocrlf` history); if so, Plan 06 owns the renormalize decision.
- **Phase 0 has its state-preview input for Mac personal.** Mac work portion will be appended by Plan 06; cross-machine deltas computed at that time.
- **Plan 04 (flameshot removal)** can now reference `00.5-state-preview.md` for the exact entryState entries (parent dir + flameshot.ini both tracked) to remove via source-delete + `rm -rf ~/.config/flameshot/` + `chezmoi apply`.
- **No blockers.** Wave 1 (Plans 02 + 03) both close cleanly; Wave 2 (Plan 04 + 05) can proceed.

## Self-Check: PASSED

Verified via `[ -f <path> ] && echo FOUND` for each file claimed, and `git log --oneline --all | grep -q <hash>` for each commit:

- FOUND: .gitattributes
- FOUND: .planning/phases/00.5-audit-documentation/00.5-state-preview.md
- FOUND: .planning/phases/00.5-audit-documentation/00.5-03-SUMMARY.md
- FOUND commit: 0bd5499 (Task 1)
- FOUND commit: 9773856 (Task 2)

---
*Phase: 00.5-audit-documentation*
*Completed: 2026-05-27*
