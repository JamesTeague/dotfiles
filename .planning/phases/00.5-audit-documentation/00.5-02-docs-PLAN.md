---
phase: 00.5-audit-documentation
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - docs/conventions.md
  - docs/dot_topics.md
autonomous: false
requirements:
  - AUD-05
must_haves:
  truths:
    - "docs/conventions.md exists and documents inherited structural decisions visible in the live tree"
    - "docs/dot_topics.md exists and documents the actual dot_topics loader convention (not the idealized 4-type model)"
    - "Both docs reference REAL tree elements (verified by grep against actual paths)"
    - "Inconsistencies discovered during inspection are FLAGGED (not normalized — flagging is Phase 0 input)"
    - "Teague has read both docs end-to-end and confirmed they match his mental model"
  artifacts:
    - path: "docs/conventions.md"
      provides: "Inherited structural decisions: .chezmoiroot, .chezmoiscripts/, .chezmoidata/, .chezmoitemplates/, .chezmoiexternal.toml refresh policy, attribute prefixes, line-ending policy"
      min_lines: 80
      contains: "chezmoiroot"
    - path: "docs/dot_topics.md"
      provides: "dot_topics/<tool>/ loader convention: load order (path → middle → completion), eval pattern, install.sh handling, attribute-prefix inconsistencies flagged"
      min_lines: 60
      contains: "dot_topics"
  key_links:
    - from: "docs/conventions.md"
      to: "docs/dot_topics.md"
      via: "internal cross-link in conventions.md when it references dot_topics convention"
      pattern: "dot_topics\\.md"
    - from: "docs/conventions.md"
      to: "PITFALLS.md Pitfall 4"
      via: "line-ending policy reference"
      pattern: "Pitfall 4|line-ending"
---

<objective>
Land the two documentation files that close AUD-05 and resolve Pitfall 22 (dot_topics undocumented). Zero apply-risk: docs/ sits at repo root OUTSIDE .chezmoiroot so chezmoi never sees it. Content is derived from inspecting the live tree per CONTEXT decision ("inspect the actual tree to derive content — do not invent conventions"). RESEARCH provides proposed shapes; this plan grounds them by re-checking the tree at execution time.

Purpose: Builds shared vocabulary for the rest of the audit AND seeds Phase 0's structural-refactor conversation with a documented baseline. The Phase 0 ".chezmoiscripts/ will become OS-nested" change reads against THIS doc's "currently FLAT" statement.

Output: Two markdown files under docs/ at repo root.
</objective>

<execution_context>
@/Users/jteague/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jteague/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/00.5-audit-documentation/00.5-CONTEXT.md
@.planning/phases/00.5-audit-documentation/00.5-RESEARCH.md
@.planning/research/PITFALLS.md
@.planning/phases/00.5-audit-documentation/00.5-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-inspect live tree and capture grounded content notes</name>
  <files>.planning/phases/00.5-audit-documentation/00.5-02-tree-notes.md</files>
  <action>
Before writing docs, re-inspect the live tree at execution time and capture findings into a working-notes file (this file is a planning-side scratchpad, NOT user-facing — but it grounds the doc content in current reality, which is the load-bearing CONTEXT decision).

Inspections to run and record:
1. ls home/.chezmoiscripts/ — enumerate scripts, note FLAT layout (no OS subdirs), note naming convention run_[once|onchange]_[before|after]_NN-name.sh.tmpl
2. ls home/.chezmoitemplates/ — enumerate partials (brew, utils, work-go-debug)
3. cat home/.chezmoiexternal.toml — record externals + refreshPeriod
4. ls home/dot_topics/ — recurse one level, record per-tool file list. SPECIFICALLY identify files lacking executable_ prefix (RESEARCH flagged rust/path.zsh and system/path.zsh.tmpl).
5. Read home/dot_zshrc.tmpl lines 30-80 — record the EXACT loader logic for dot_topics: which extensions sourced, in what order, completion.zsh last.
6. ls home/ at the source root — record dot_X / private_X / executable_X / exact_X / modify_X attribute-prefix examples present in THIS repo.
7. cat .chezmoiroot — confirm content is "home" (or "./home/")
8. find . -maxdepth 2 -type d | head — confirm what sits at REPO root outside .chezmoiroot (keyboard/, .planning/, docs/ once created, README.md as file)

Write findings as a flat markdown notes file with a section per inspection. This file is referenced by Tasks 2 and 3 so they cite real findings, not invented ones.

If any inspection finding CONTRADICTS RESEARCH's proposed shape (e.g., dot_topics tree shape differs from what RESEARCH described), STOP and flag in the notes — Task 2/3 then describe REALITY and add a "Known Inconsistency" callout.
  </action>
  <verify>
    <automated>[[ -f .planning/phases/00.5-audit-documentation/00.5-02-tree-notes.md ]] && grep -q chezmoiscripts .planning/phases/00.5-audit-documentation/00.5-02-tree-notes.md && grep -q dot_topics .planning/phases/00.5-audit-documentation/00.5-02-tree-notes.md && grep -q chezmoiexternal .planning/phases/00.5-audit-documentation/00.5-02-tree-notes.md</automated>
  </verify>
  <done>Notes file exists with all 8 inspection sections populated against the actual live tree at execution time.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write docs/conventions.md</name>
  <files>docs/conventions.md</files>
  <behavior>
Doc must satisfy:
- assert_file docs/conventions.md → PASS
- grep -q chezmoiroot → PASS (RESEARCH-mandated keyword)
- grep -q "FLAT" or "flat" (current .chezmoiscripts layout) → PASS
- grep -q "168h" or "refreshPeriod" (externals policy) → PASS
- grep -q "modify_" (modify_dot_gitconfig.local pattern) → PASS
- grep -q "keyboard" (top-level non-managed dir) → PASS
- File length min 80 lines (depth signal — shorter likely missing required sections)
  </behavior>
  <action>
Write docs/conventions.md. Source content from the tree-notes file (Task 1). Required sections per RESEARCH "docs/conventions.md proposed shape":

1. ".chezmoiroot" — what it is, value here (home/), implication that everything outside home/ is invisible to chezmoi. Enumerate what sits OUTSIDE: keyboard/, README.md, .planning/, docs/ itself.
2. ".chezmoiscripts/" — current FLAT layout. List the 6 scripts (from Task 1 notes). Naming convention run_[once|onchange]_[before|after]_NN-name.sh.tmpl. Numeric prefix orders. NOTE: will become OS-nested in Phase 0 (forward-reference).
3. ".chezmoidata/" — plain YAML, NO templating allowed inside (loaded before template engine starts; chezmoi limitation). Enumerate files present (packages.yaml, tmux-languages.yaml, tmux-tools.yaml).
4. ".chezmoitemplates/" — partials invoked via {{ template "name" . }}. Enumerate current partials (brew, utils, work-go-debug) with one-line purpose each.
5. ".chezmoiexternal.toml" — externals refresh policy. refreshPeriod = "168h" weekly. Override with chezmoi apply --refresh-externals.
6. "dot_topics/<tool>/" convention — one-paragraph summary + link to docs/dot_topics.md.
7. "Attribute prefixes used in THIS repo" — dot_X, private_X, executable_X, exact_X, modify_X — for each, one-line description and the literal example present in this repo (e.g., "modify_ — modify_dot_gitconfig.local appends a [user] block per machine without overwriting existing content").
8. "Line endings" — LF in source via .gitattributes. Render-time control per-file via chezmoi:template:line-ending=native directive (Phase 2 territory). Link to PITFALLS.md Pitfall 4.
9. "Canonical 'is my repo clean?' command" — chezmoi diff -x externals (per RESEARCH Pitfall A; externals weekly churn is a false alarm).

Use H2 for each numbered section. Cross-link to docs/dot_topics.md in section 6. Cite real file paths verbatim (no placeholders).

If tree-notes flagged inconsistencies, add a final H2 "Known inconsistencies (flagged for Phase 0 normalization)" listing them — DO NOT silently normalize.
  </action>
  <verify>
    <automated>[[ -f docs/conventions.md ]] && grep -q chezmoiroot docs/conventions.md && (grep -q "FLAT\|flat" docs/conventions.md) && grep -q "168h\|refreshPeriod" docs/conventions.md && grep -q "modify_" docs/conventions.md && grep -q "keyboard" docs/conventions.md && [[ $(wc -l < docs/conventions.md) -ge 80 ]]</automated>
  </verify>
  <done>docs/conventions.md exists with all 9 required sections + (if applicable) flagged inconsistencies section. Passes grep + length checks.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write docs/dot_topics.md</name>
  <files>docs/dot_topics.md</files>
  <behavior>
Doc must satisfy:
- assert_file docs/dot_topics.md → PASS
- grep -q dot_topics → PASS
- grep -q "path.zsh" → PASS (file-type convention)
- grep -q "completion.zsh" → PASS (load-order discussion)
- grep -q "install.sh" → PASS (NOT auto-sourced; invoked by run_onchange script)
- grep -q "executable_" → PASS (attribute-prefix inconsistency flagged)
- File length min 60 lines
  </behavior>
  <action>
Write docs/dot_topics.md describing what is ACTUALLY in dot_topics/ tree (per Task 1 notes), not the idealized 4-type model from CONTEXT. Required sections per RESEARCH "docs/dot_topics.md proposed shape":

1. "What it is" — the loader pattern wired through home/dot_zshrc.tmpl. Quote the line range from the tree-notes (sourcing pass: path first, middle pass, completion last). Explain WHY load order matters (PATH before evals; completion after compinit).
2. "File-type conventions present in this tree" — markdown table:
   - path.zsh — sourced FIRST (sets $PATH / PATH-adjacent env)
   - completion.zsh — sourced LAST (after compinit)
   - aliases.zsh, config.zsh — middle pass, no special treatment beyond extension
   - eval.zsh — middle-pass *.zsh that evals a tool's hook (examples: atuin, starship)
   - install.sh — NOT auto-sourced. Extension is .sh; invoked by .chezmoiscripts/run_onchange_after_03-install-topics.sh.tmpl which finds install.sh files.
3. "Attribute prefixes observed" — executable_ widely used; rust/path.zsh and system/path.zsh.tmpl LACK the executable_ prefix. Flag as known inconsistency; do NOT normalize in Phase 0.5 (would change destination file mode → chezmoi diff non-zero). Leave for Phase 0.
4. "Template files inside dot_topics/" — dotnet/executable_path.zsh.tmpl and system/path.zsh.tmpl. Note .tmpl works inside dot_topics/ and renders normally before the loader sources the file.
5. "Adding a new tool" — concrete 4-line recipe:
   - mkdir home/dot_topics/<tool>/
   - create executable_path.zsh if it adjusts PATH (sourced first)
   - create executable_eval.zsh if it has a hook to eval (sourced middle)
   - create install.sh if it needs an install step (NOT auto-sourced; will run via run_onchange_after_03)
6. (Optional) Cross-link back to docs/conventions.md.

Use literal file paths from the live tree. Include the load-order quote from dot_zshrc.tmpl.
  </action>
  <verify>
    <automated>[[ -f docs/dot_topics.md ]] && grep -q dot_topics docs/dot_topics.md && grep -q "path.zsh" docs/dot_topics.md && grep -q "completion.zsh" docs/dot_topics.md && grep -q "install.sh" docs/dot_topics.md && grep -q "executable_" docs/dot_topics.md && [[ $(wc -l < docs/dot_topics.md) -ge 60 ]]</automated>
  </verify>
  <done>docs/dot_topics.md exists with all 5 required sections + cross-link. Passes grep + length checks.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Teague reads both docs end-to-end for accuracy</name>
  <files>(checkpoint — human action required; no files written by Claude in this task)</files>
  <action>
WHAT WAS BUILT (context for the human):
docs/conventions.md and docs/dot_topics.md derived from live-tree inspection.

HOW TO VERIFY (steps for the human to perform):
1. Open docs/conventions.md and read it against the actual tree. Spot-check claims:
   - .chezmoiroot section claim matches `cat .chezmoiroot`
   - .chezmoiscripts script list matches `ls home/.chezmoiscripts/`
   - .chezmoitemplates partials list matches `ls home/.chezmoitemplates/`
   - Attribute-prefix examples are present at the cited paths
2. Open docs/dot_topics.md and read it against `ls -R home/dot_topics/`:
   - Load-order claim matches actual dot_zshrc.tmpl behavior
   - executable_ inconsistency is flagged for rust/ and system/ (current truth)
   - install.sh handling is correctly described (invoked by run_onchange_after_03-install-topics.sh.tmpl)
3. Report any claim that doesn't match what you actually do or what's actually in the tree.

This is human judgment — grep can confirm strings exist, not that they're true.
  </action>
  <verify>
    <automated>MISSING — human-in-loop checkpoint; verification is the resume-signal paste recorded in the next autonomous task. Downstream task will assert on the file(s) the human action produced.</automated>
  </verify>
  <done>Human completed the verify steps above AND provided the resume signal: Type "approved" if both docs match reality. Otherwise describe specific corrections needed; planner re-runs Tasks 2-3 with edits.</done>
</task>

</tasks>

<verification>
After all tasks:
- bash .planning/phases/00.5-audit-documentation/checks/quick.sh — AUD-05 section now PASSes (was PENDING in Plan 01)
- Teague checkpoint approved
- git diff --stat shows docs/conventions.md + docs/dot_topics.md as NEW files; no other source touched
</verification>

<success_criteria>
- docs/conventions.md exists, min 80 lines, contains all 9 required sections + flagged inconsistencies (if any)
- docs/dot_topics.md exists, min 60 lines, contains all 5 required sections + cross-link
- quick.sh AUD-05 section PASSes
- Teague checkpoint approved (docs match reality)
- No source-tree mutation outside docs/ (zero chezmoi diff impact)
</success_criteria>

<output>
Create .planning/phases/00.5-audit-documentation/00.5-02-SUMMARY.md documenting:
- Both docs created with their key sections
- Any inconsistencies flagged for Phase 0 (Phase 0 input)
- Tree-notes findings that should inform Plan 05's packages.yaml work
</output>
