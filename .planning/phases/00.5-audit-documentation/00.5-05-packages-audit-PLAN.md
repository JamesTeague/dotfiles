---
phase: 00.5-audit-documentation
plan: 05
type: execute
wave: 3
depends_on: [01, 03]
files_modified:
  - .planning/phases/00.5-audit-documentation/00.5-packages-candidates.md
  - .planning/phases/00.5-audit-documentation/checks/approved-brew-bundle.txt
  - home/.chezmoidata/packages.yaml
autonomous: false
requirements:
  - AUD-01
  - AUD-02
must_haves:
  truths:
    - "00.5-packages-candidates.md populated with two-signal cross-check rows (installed + upstream status) for every candidate"
    - "Teague reviewed the candidate list and marked each row with delete/keep/move/defer"
    - "packages.yaml LIGHT normalization committed per AUD-02 (rename/move only — NO splitting)"
    - "Approved brew-bundle snapshot captured to checks/approved-brew-bundle.txt"
    - "chezmoi execute-template < run_onchange_before_02-install-packages.sh.tmpl matches the approved snapshot byte-for-byte"
    - "All entries marked delete in candidate list are pruned from packages.yaml in this plan's commit"
    - "All entries in candidate list have a final decision (no rows left unmarked)"
  artifacts:
    - path: ".planning/phases/00.5-audit-documentation/00.5-packages-candidates.md"
      provides: "Populated candidate-removal list with Teague decisions and Final Commit Manifest filled in"
      contains: "Final Commit Manifest"
    - path: ".planning/phases/00.5-audit-documentation/checks/approved-brew-bundle.txt"
      provides: "Snapshot of rendered brew-bundle output after normalization — full.sh diffs against this"
    - path: "home/.chezmoidata/packages.yaml"
      provides: "Light-normalized packages.yaml: dead entries pruned per Teague's decisions, personal/work nesting fixed within current shape, NO axis restructure (Phase 0 territory)"
  key_links:
    - from: "home/.chezmoidata/packages.yaml"
      to: ".planning/phases/00.5-audit-documentation/checks/approved-brew-bundle.txt"
      via: "chezmoi execute-template render of home/.chezmoiscripts/run_onchange_before_02-install-packages.sh.tmpl"
      pattern: "execute-template.*02-install-packages"
    - from: "Teague decisions in candidates.md"
      to: "Final Commit Manifest section"
      via: "explicit decision aggregation"
      pattern: "Final Commit Manifest"
---

<objective>
Execute AUD-01 (dead packages.yaml entries removed via human-reviewed candidate list) + AUD-02 (LIGHT personal/work nesting normalization — rename/move only, NO splitting). The two requirements are combined here because they touch the same file (packages.yaml) and the normalization moves dead-path entries (personal.casks, work.core) into template-consumed paths (personal.darwin.*, work.darwin.*) — which is BOTH a normalization AND an effective dead-code activation. Combining the work prevents double-churn of the install-packages script's run_onchange hash.

CONTEXT decisions enforced:
- AUD-02 is LIGHT: rename/move only, NO file splitting. Heavier cases flagged for Phase 0.
- AUD-01 dead determination is NOT autonomous: candidate list reviewed by Teague before commit.
- Per RESEARCH § AUD-02 concrete asymmetries: personal.casks (mullvadvpn, kindle, handbrake, rode-central) is dead at template level — move to personal.darwin.casks. work.core is dead at template level — move items to work.darwin.{taps,brews,casks}. Linux empty placeholders: keep (per Open Q 5; removal requires brew template guard rewrite — Phase 0 territory).
- Pitfall D: run_onchange_02-install-packages.sh.tmpl will re-fire on next apply (expected, idempotent). Pre-verify with chezmoi execute-template before commit so there are no surprise installs.

Output: Populated candidate list with decisions, normalized packages.yaml, approved-brew-bundle snapshot.
</objective>

<execution_context>
@/Users/jteague/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jteague/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00.5-audit-documentation/00.5-CONTEXT.md
@.planning/phases/00.5-audit-documentation/00.5-RESEARCH.md
@.planning/phases/00.5-audit-documentation/00.5-VALIDATION.md
@.planning/phases/00.5-audit-documentation/00.5-packages-candidates.md
@.planning/phases/00.5-audit-documentation/00.5-02-SUMMARY.md
@.planning/phases/00.5-audit-documentation/00.5-03-SUMMARY.md
@home/.chezmoidata/packages.yaml
@home/.chezmoitemplates/brew
@home/.chezmoiscripts/run_onchange_before_02-install-packages.sh.tmpl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate candidate-removal list with two-signal cross-check</name>
  <files>.planning/phases/00.5-audit-documentation/00.5-packages-candidates.md</files>
  <action>
Populate every empty table in the scaffold. Methodology per CONTEXT:
- Signal A: installed? — brew list --formula and brew list --cask on Mac personal
- Signal B: upstream maintenance — brew info <pkg> | head -20; flag "deprecated", "disabled", or stale "last-bottled" date older than 2 years
- Both signals together (NOT installed AND NOT maintained) → recommendation: delete
- Either signal alone → recommendation: review (Teague decides)

Generate the candidate set from home/.chezmoidata/packages.yaml using python3 or yq. Walk the YAML and emit rows for every (block, type, name) tuple across: top-level packages.{core,darwin,linux}.{taps,brews,casks}, personal.* (top + darwin + linux), work.* (core + darwin + linux).

For each row, fill:
- Type: tap / brew / cask
- Name: literal name from YAML
- Block (current): YAML path (e.g., personal.casks, work.core.brews)
- Installed on Mac personal?: yes/no (from brew list)
- Upstream status: maintained / deprecated / disabled / "last bottled YYYY-MM-DD"
- Recommendation: delete (both signals red) / review (one signal red) / keep (both green) / move (the personal.casks → personal.darwin.casks AUD-02 case — recommend "move" not "delete")
- Decision: LEAVE EMPTY (Teague fills in Task 2)

Special rows to ALWAYS include in the appropriate dead-path table (per RESEARCH):
- work.core.{taps,brews,casks} entries — recommendation always "move to work.darwin.<type>" because the template never reaches work.core
- personal.{taps,brews,casks} top-level entries (mullvadvpn, kindle, handbrake, rode-central per RESEARCH) — recommendation always "move to personal.darwin.casks" because the template never reaches personal.casks (these items ARE installed on the machine — not delete candidates)

Empty placeholders table — per RESEARCH Open Q 5, recommendation is "keep" with note "brew template ranges over these; removal requires guard rewrite (Phase 0)".

Final Commit Manifest section — leave with a placeholder line "(Teague fills this in Task 2 after marking decisions)".

After populating, verify with `cat 00.5-packages-candidates.md | grep -c '^|'` — should be substantially more rows than the empty scaffold.
  </action>
  <verify>
    <automated>grep -q 'mullvadvpn' .planning/phases/00.5-audit-documentation/00.5-packages-candidates.md && grep -q 'work.core' .planning/phases/00.5-audit-documentation/00.5-packages-candidates.md && [[ $(grep -c '^|' .planning/phases/00.5-audit-documentation/00.5-packages-candidates.md) -ge 20 ]]</automated>
  </verify>
  <done>All four candidate tables populated with two-signal cross-check data. Dead-path moves explicitly recommended (personal.casks → personal.darwin.casks; work.core → work.darwin). Decision column empty for Teague.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Teague reviews candidate list and marks decisions</name>
  <files>(checkpoint — human action required; no files written by Claude in this task)</files>
  <action>
WHAT WAS BUILT (context for the human):
00.5-packages-candidates.md populated with cross-check data. Decision column empty.

HOW TO VERIFY (steps for the human to perform):
1. Open .planning/phases/00.5-audit-documentation/00.5-packages-candidates.md
2. For EACH row, mark the Decision column with one of: delete / keep / move / defer
   - delete: prune from packages.yaml in this plan's commit
   - keep: leave as-is (dormant but wanted; document WHY in a note column if needed)
   - move: relocate within packages.yaml (the personal.casks → personal.darwin.casks and work.core → work.darwin.* moves are the AUD-02 normalization)
   - defer: flag for Phase 0 or later (do NOT touch in 0.5)
3. Populate the "Final Commit Manifest" section as a flat list of operations Task 3 will execute. Example format:

   DELETE:
   - personal.casks: snyk-cli (deprecated upstream, not installed)
   - ...

   MOVE:
   - personal.casks → personal.darwin.casks: mullvadvpn, kindle, handbrake, rode-central
   - work.core.taps → work.darwin.taps: snyk/tap, localstack/tap
   - work.core.brews → work.darwin.brews: synk, node, awscli, ...
   - work.core.casks → work.darwin.casks: postman, microsoft-teams
   - After moves, delete the empty work.core block

   KEEP (no action):
   - linux.{taps,casks} empty placeholders (per Open Q 5 — Phase 0 territory)
   - ...

   DEFER:
   - ...

4. Save the file. Confirm every row has a decision (no blank Decision cells).

Sanity check: the AUD-02 LIGHT scope means MOVE operations only relocate within the current personal/work axis. Any decision that requires splitting personal/work into the role-based axes is DEFER (Phase 0 TAX-05).
  </action>
  <verify>
    <automated>MISSING — human-in-loop checkpoint; verification is the resume-signal paste recorded in the next autonomous task. Downstream task will assert on the file(s) the human action produced.</automated>
  </verify>
  <done>Human completed the verify steps above AND provided the resume signal: Type "approved" once every row has a decision and the Final Commit Manifest is populated. If you want adjustments to the candidate set, describe — Task 1 re-runs.</done>
</task>

<task type="auto">
  <name>Task 3: Execute Final Commit Manifest + capture approved-brew-bundle snapshot</name>
  <files>home/.chezmoidata/packages.yaml, .planning/phases/00.5-audit-documentation/checks/approved-brew-bundle.txt</files>
  <action>
Parse the "Final Commit Manifest" section of 00.5-packages-candidates.md and execute each DELETE and MOVE operation against home/.chezmoidata/packages.yaml. Use Python (PyYAML) for the edits so the result is structurally valid and round-trips cleanly:

  python3 -c "
  import yaml
  with open('home/.chezmoidata/packages.yaml') as f:
      data = yaml.safe_load(f)
  # apply each operation per the manifest
  # ...
  with open('home/.chezmoidata/packages.yaml', 'w') as f:
      yaml.safe_dump(data, f, sort_keys=False, default_flow_style=False)
  "

NOTE on YAML preservation: PyYAML's safe_dump may re-order keys or normalize quoting. If the existing file uses specific stylistic choices (single-quoted strings, key ordering matching the schema), consider ruamel.yaml (round-trip preservation) instead. Verify with `git diff home/.chezmoidata/packages.yaml` that ONLY the intended structural changes happened — no whitespace-only or quote-style churn.

If ruamel.yaml is not installed, hand-edit the file (it is 142 lines, AUD-02 scope is rename/move only — manual edits are tractable). The Final Commit Manifest provides the exact operations.

After edits:

SUB-STEP A — Structural validation:
  python3 -c "import yaml; yaml.safe_load(open('home/.chezmoidata/packages.yaml'))"
  # Exit 0 = valid YAML

SUB-STEP B — Render and snapshot:
  chezmoi execute-template < home/.chezmoiscripts/run_onchange_before_02-install-packages.sh.tmpl > /tmp/00.5-new-rendered-brew.txt
  # Read the rendered brew bundle. Cross-check against `brew list --formula` and `brew list --cask`:
  # - Every brew/cask in the rendered output should already be installed on Mac personal (or be a deliberate add via the move operation).
  # - The personal.darwin.casks block should now include the moved items (mullvadvpn etc).
  # - The work.darwin.* blocks should include items moved from work.core.
  # - work.core block is gone OR is empty (depending on manifest).

SUB-STEP C — Commit the approved snapshot:
  cp /tmp/00.5-new-rendered-brew.txt .planning/phases/00.5-audit-documentation/checks/approved-brew-bundle.txt

This snapshot is the reference that full.sh diffs against. Future plans (Phase 0) regenerate and approve a new snapshot when packages.yaml legitimately changes.

SUB-STEP D — Dry-run apply to verify run_onchange will re-fire (expected):
  chezmoi apply --dry-run --verbose 2>&1 | grep -E '02-install-packages|run script' | head -20
  # Expect the install-packages script to be listed as pending-rerun. This is Pitfall D — expected and idempotent (brew bundle re-runs no-op if everything is installed).

SUB-STEP E — Commit:
  git add home/.chezmoidata/packages.yaml .planning/phases/00.5-audit-documentation/checks/approved-brew-bundle.txt .planning/phases/00.5-audit-documentation/00.5-packages-candidates.md
  git commit -m "AUD-01 + AUD-02: prune dead packages.yaml entries; normalize personal/work nesting (LIGHT — rename/move only)"

If chezmoi execute-template fails OR brew bundle output references a package that is NOT installed AND was NOT a deliberate add via a MOVE operation, STOP — there's an error in the manifest interpretation. Surface for review.
  </action>
  <verify>
    <automated>python3 -c "import yaml; yaml.safe_load(open('home/.chezmoidata/packages.yaml'))" && [[ -f .planning/phases/00.5-audit-documentation/checks/approved-brew-bundle.txt ]] && chezmoi execute-template < home/.chezmoiscripts/run_onchange_before_02-install-packages.sh.tmpl > /tmp/00.5-verify-rendered.txt && diff /tmp/00.5-verify-rendered.txt .planning/phases/00.5-audit-documentation/checks/approved-brew-bundle.txt</automated>
  </verify>
  <done>packages.yaml passes YAML validation. Rendered brew template byte-matches the approved snapshot. Commit landed referencing AUD-01 and AUD-02. Final Commit Manifest in candidates.md drives the actual changes (no manifest entries skipped, no unmanifest changes).</done>
</task>

</tasks>

<verification>
After all tasks:
- bash .planning/phases/00.5-audit-documentation/checks/quick.sh — AUD-01/02 section now PASSes (valid YAML + shottr still present)
- bash .planning/phases/00.5-audit-documentation/checks/full.sh --no-diff-gate — passes through to step 5 (approved snapshot diff) which now also PASSes
- git log --oneline -3 — shows the AUD-01+02 commit
- Manual: open candidates.md, every row has a Decision; Final Commit Manifest populated
</verification>

<success_criteria>
- 00.5-packages-candidates.md fully populated, every Decision marked, Final Commit Manifest filled
- home/.chezmoidata/packages.yaml passes YAML validation
- approved-brew-bundle.txt exists; chezmoi execute-template matches it byte-for-byte
- Commit message references AUD-01 + AUD-02
- No restructuring beyond LIGHT moves (no axis changes — Phase 0 territory respected)
- quick.sh AUD-01/02 sections PASS
- full.sh --no-diff-gate steps 4 + 5 PASS
</success_criteria>

<output>
Create .planning/phases/00.5-audit-documentation/00.5-05-SUMMARY.md documenting:
- Candidate counts (total / delete / keep / move / defer)
- Operations executed (DELETE list, MOVE list)
- Approved snapshot SHA + size
- Pre-existing drift status (DEBUG=1 .zshrc untouched — Plan 06 territory)
- Any items deferred to Phase 0 with rationale
</output>
