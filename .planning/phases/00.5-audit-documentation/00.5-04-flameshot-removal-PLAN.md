---
phase: 00.5-audit-documentation
plan: 04
type: execute
wave: 3
depends_on: [01, 03]
files_modified:
  - home/private_dot_config/flameshot/flameshot.ini
autonomous: false
requirements:
  - AUD-03
must_haves:
  truths:
    - "home/private_dot_config/flameshot/ source directory is removed"
    - "Destination ~/.config/flameshot/ on Mac personal is also removed (per Pitfall C resolution)"
    - "chezmoi state entryState no longer contains ~/.config/flameshot/flameshot.ini after cleanup"
    - "chezmoi diff -x externals contribution from flameshot removal is zero (destination matches source — both absent)"
  artifacts:
    - path: "home/private_dot_config/flameshot/"
      provides: "directory absent (Phase 0.5 deletion target)"
      contains: "(must NOT exist)"
  key_links:
    - from: "source delete (home/private_dot_config/flameshot/)"
      to: "destination cleanup (~/.config/flameshot/)"
      via: "manual rm -rf on Mac personal after source delete + chezmoi apply to update entryState"
      pattern: "rm -rf.*\\.config/flameshot"
---

<objective>
Execute AUD-03: delete orphaned home/private_dot_config/flameshot/ source directory AND clean up the destination ~/.config/flameshot/ on Mac personal so chezmoi entryState no longer tracks it. Per RESEARCH Pitfall C: source deletion alone leaves an orphan in chezmoi state and a non-empty diff — destination cleanup is REQUIRED for the exit gate.

Phase 0 will gate the flameshot config to role=dev + os=linux + not wsl via templated .chezmoiignore (a fresh flameshot config block will land THEN, when Linux laptop materializes). The 0.5 work cleanly removes the orphan; it does NOT pre-stage the future role-gated version.

Output: Source directory deleted; destination directory absent on Mac personal; entryState clean.
</objective>

<execution_context>
@/Users/jteague/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jteague/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00.5-audit-documentation/00.5-CONTEXT.md
@.planning/phases/00.5-audit-documentation/00.5-RESEARCH.md
@.planning/phases/00.5-audit-documentation/00.5-state-preview.md
@.planning/phases/00.5-audit-documentation/00.5-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm pre-state and capture baseline</name>
  <files>.planning/phases/00.5-audit-documentation/00.5-04-flameshot-baseline.md</files>
  <action>
Before any destructive operation, capture baseline (planning-side notes file, not user-facing):

1. ls -la home/private_dot_config/flameshot/ — confirm flameshot.ini is the only file; record its size (per RESEARCH: 147 bytes)
2. ls -la ~/.config/flameshot/ — confirm destination exists with flameshot.ini
3. chezmoi managed | grep flameshot — confirm chezmoi tracks the path
4. chezmoi state get-bucket --bucket=entryState | grep -i flameshot — confirm entryState entry exists
5. cat home/private_dot_config/flameshot/flameshot.ini — preserve content in baseline notes (in case it's wanted later as Phase 0 input for the role=dev+linux gating)

Write all findings into .planning/phases/00.5-audit-documentation/00.5-04-flameshot-baseline.md so the deletion is reversible from notes alone if needed.

If destination ~/.config/flameshot/ already absent (e.g., user manually deleted it earlier), proceed to source-only deletion + state cleanup. Document the discrepancy.

If chezmoi managed does NOT show flameshot, STOP and surface — the assumption that it's tracked is wrong; surface as a Phase 0.5 blocker for human review.
  </action>
  <verify>
    <automated>[[ -f .planning/phases/00.5-audit-documentation/00.5-04-flameshot-baseline.md ]] && grep -q 'flameshot.ini' .planning/phases/00.5-audit-documentation/00.5-04-flameshot-baseline.md && grep -q 'entryState' .planning/phases/00.5-audit-documentation/00.5-04-flameshot-baseline.md</automated>
  </verify>
  <done>Baseline notes file captures source size, destination presence, chezmoi tracking, entryState entry, and the .ini content for posterity.</done>
</task>

<task type="auto">
  <name>Task 2: Delete source directory + commit</name>
  <files>home/private_dot_config/flameshot/flameshot.ini</files>
  <action>
Delete the source directory:
  git rm -r home/private_dot_config/flameshot/

Verify removal:
  [[ ! -d home/private_dot_config/flameshot ]]

Run chezmoi diff -x externals to see what chezmoi wants to do with the orphan:
  chezmoi diff -x externals

Expected per Pitfall C: chezmoi shows the destination as either "would remove" or "orphaned" depending on [remove] config. On Mac personal there is no [remove] config (~/.config/chezmoi/chezmoi.toml is 98 bytes — just [data]). Default behavior: chezmoi shows the entry as orphaned in chezmoi managed --include=files but does NOT automatically remove the destination on apply.

Commit the source delete:
  git commit -m "AUD-03: delete orphaned flameshot source directory (Shottr replaced it on Mac)"

NOTE: This commit alone does NOT zero the chezmoi diff — destination cleanup happens in Task 3. The exit gate is across the whole phase, not per-task.
  </action>
  <verify>
    <automated>[[ ! -d home/private_dot_config/flameshot ]] && git log -1 --pretty=format:%s | grep -q 'AUD-03' && git status --porcelain | grep -v '^??' | wc -l | xargs -I{} test {} -eq 0</automated>
  </verify>
  <done>Source directory deleted via git rm; commit landed with AUD-03 message; working tree clean (no staged or unstaged changes); only untracked files (if any) remain.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm destination cleanup approach + execute on Mac personal</name>
  <files>(checkpoint — human action required; no files written by Claude in this task)</files>
  <action>
WHAT WAS BUILT (context for the human):
Source directory deleted (Task 2). Destination ~/.config/flameshot/ still present on Mac personal — needs cleanup to zero the chezmoi diff.

HOW TO VERIFY (steps for the human to perform):
Per RESEARCH Pitfall C, the cleanest one-shot approach is:

OPTION A (recommended): Manual destination delete + chezmoi apply to update state
  rm -rf ~/.config/flameshot/
  chezmoi apply
  chezmoi state get-bucket --bucket=entryState | grep -i flameshot   # expect empty
  chezmoi diff -x externals                                            # expect flameshot no longer mentioned (DEBUG=1 pre-existing drift may still appear — that's Plan 06)

OPTION B: Add a per-machine [remove] rule to ~/.config/chezmoi/chezmoi.toml (heavier — not recommended for a one-shot)

OPTION C: chezmoi forget the path (clears state without filesystem touch — leaves destination as orphan on disk)

Run OPTION A unless you specifically want B or C. Paste the output of the entryState check + chezmoi diff -x externals (the flameshot portion only — pre-existing .zshrc drift is OK to show, it's Plan 06's problem).

If chezmoi apply prompts for anything other than the flameshot removal, STOP and report — there's unexpected pending work.
  </action>
  <verify>
    <automated>MISSING — human-in-loop checkpoint; verification is the resume-signal paste recorded in the next autonomous task. Downstream task will assert on the file(s) the human action produced.</automated>
  </verify>
  <done>Human completed the verify steps above AND provided the resume signal: Type "approved" with the entryState empty-confirmation pasted in. If issues, describe — planner figures out the recovery path.</done>
</task>

</tasks>

<verification>
After all tasks:
- bash .planning/phases/00.5-audit-documentation/checks/quick.sh — AUD-03 section now PASSes (assert_dir_missing on home/private_dot_config/flameshot)
- Manual: chezmoi state get-bucket --bucket=entryState | grep -i flameshot returns empty
- Manual: chezmoi diff -x externals — flameshot portion is gone (only pre-existing .zshrc DEBUG=1 drift may remain, Plan 06 territory)
</verification>

<success_criteria>
- home/private_dot_config/flameshot/ deleted from source (git tree)
- ~/.config/flameshot/ deleted from Mac personal destination
- entryState no longer tracks flameshot.ini
- Source delete + cleanup actions committed
- quick.sh AUD-03 section PASSes
</success_criteria>

<output>
Create .planning/phases/00.5-audit-documentation/00.5-04-SUMMARY.md documenting:
- Source delete commit SHA
- Destination cleanup option chosen (A/B/C) and result
- entryState verification result
- Reference to baseline notes (recoverable .ini content + sizes) so Phase 0's role=dev+linux gating can re-stage if/when needed
</output>
