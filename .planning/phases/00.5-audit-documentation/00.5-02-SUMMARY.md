---
phase: 00.5-audit-documentation
plan: 02
subsystem: docs
tags: [documentation, chezmoi-conventions, dot_topics, loader-pattern, attribute-prefixes, line-endings, phase-0-input]

requires:
  - phase: 00.5-audit-documentation
    provides: "Wave 0 harness (quick.sh AUD-05 section as PENDING placeholder) + live-tree truth from Plan 01 inspection scaffolding"
provides:
  - "docs/conventions.md (173 lines) — inherited structural decisions documented end-to-end: .chezmoiroot, .chezmoiscripts/ (currently FLAT), .chezmoidata/, .chezmoitemplates/, .chezmoiexternal.toml refresh policy, dot_topics/ link, attribute prefixes (dot_/private_/executable_/exact_/modify_), line-ending policy, canonical 'is my repo clean?' command"
  - "docs/dot_topics.md (157 lines) — the actual loader pattern wired through home/dot_zshrc.tmpl (path → middle → completion), file-type conventions table, executable_ prefix inconsistencies flagged (rust/path.zsh, system/path.zsh.tmpl), install.sh handling (NOT auto-sourced; invoked by run_onchange_after_03), template files inside dot_topics/, 4-line recipe for adding a new tool"
  - "Tree-notes scratchpad (.planning/phases/00.5-audit-documentation/00.5-02-tree-notes.md, 257 lines) — 8-section live-tree inspection grounding the docs in REALITY, not RESEARCH's idealized 4-type model"
  - "Documented baseline for Phase 0's structural-refactor conversation — the conventions doc's 'currently FLAT' statement is the reference point the .chezmoiscripts/ OS-nesting change will read against"
  - "Pitfall 22 (dot_topics undocumented) resolved"
  - "Task 4 attestation (00.5-02-attestation.md) — Teague's 'approved' resume signal recorded with timestamp + paths reviewed + spot-checks performed, per Task 4's human-verify done criterion"
affects:
  - "00.5-04-flameshot-removal (now has docs/conventions.md to cite for chezmoi's no-auto-prune-empty-dirs behavior)"
  - "00.5-05-packages-audit (now has docs/conventions.md .chezmoidata/ section establishing the 'no templating in .chezmoidata/' constraint)"
  - "00.5-06-exit-gate (canonical 'chezmoi diff -x externals' command now lives in docs/conventions.md instead of being repeated per-plan)"
  - "phase-0-refactor (TAX-* requirements consume docs/conventions.md as 'before' reference; the FLAT .chezmoiscripts/ statement is the load-bearing baseline)"
  - "phase-2-windows (line-ending policy + chezmoi:template:line-ending=native directive now documented in docs/conventions.md before Windows-side work begins)"

tech-stack:
  added: []
  patterns:
    - "Documentation grounded in live-tree inspection, not RESEARCH speculation — Task 1 inspections feed Tasks 2-3 verbatim; if reality contradicts RESEARCH, doc describes REALITY and adds a 'Known Inconsistencies' callout (FLAG, do NOT silently normalize)"
    - "docs/ lives at repo root OUTSIDE .chezmoiroot=home — zero apply-risk; chezmoi never sees it"
    - "Cross-doc linking: conventions.md section 6 links to dot_topics.md; both link back to PITFALLS.md Pitfall 4 for line-endings"
    - "Human-verify checkpoint attestation pattern: resume signal + timestamp + paths reviewed + per-step spot-checks recorded as a committable artifact (00.5-02-attestation.md), giving Phase 0 traceable evidence that this doc was reviewed against reality at a known point in time"

key-files:
  created:
    - "docs/conventions.md (173 lines, 10 H2 sections including the 9 RESEARCH-mandated + 'Known inconsistencies' callout)"
    - "docs/dot_topics.md (157 lines, 6 sections covering loader pattern + file-type conventions + attribute-prefix inconsistencies + template-files inside dot_topics + add-a-tool recipe + cross-link)"
    - ".planning/phases/00.5-audit-documentation/00.5-02-tree-notes.md (257 lines, 8 inspection sections — planning-side scratchpad referenced by Tasks 2-3; NOT user-facing)"
    - ".planning/phases/00.5-audit-documentation/00.5-02-attestation.md (49 lines, Task 4 human-verify resume-signal record)"
  modified: []

key-decisions:
  - "Both docs describe REALITY, not the idealized model from RESEARCH/CONTEXT. dot_topics.md notably documents the OBSERVED file-type set (path.zsh, completion.zsh, aliases.zsh, config.zsh, eval.zsh, install.sh) rather than the 4-type model CONTEXT cited ({path,eval,config,install}.zsh|sh) — the observed set is a superset, flagged accordingly."
  - "Attribute-prefix inconsistencies (rust/path.zsh and system/path.zsh.tmpl lacking executable_ prefix) FLAGGED in both docs as known inconsistencies — deliberately NOT normalized in Phase 0.5 because renaming to executable_ would change destination file mode, breaking chezmoi diff = zero exit-gate. Defer to Phase 0 with the rest of the structural-refactor work."
  - "docs/conventions.md section 2 (.chezmoiscripts/) explicitly states 'currently FLAT' and forward-references Phase 0's OS-nesting refactor. This is load-bearing: Phase 0's diff against the doc will be the explicit before/after for AUD's structural baseline."
  - "Skip-renormalize decision from Plan 03 captured in docs/conventions.md line-endings section, per Plan 03's commit-message commitment ('canonical policy statement deferred to docs/conventions.md')."
  - "Canonical 'is my repo clean?' command (`chezmoi diff -x externals`) lives in conventions.md section 9 — per RESEARCH Pitfall A, externals' weekly refresh churn is a false alarm; the -x flag makes the exit-gate signal trustworthy."
  - "Task 4 (human-verify) approval recorded as a committed artifact (00.5-02-attestation.md) rather than only in commit message — gives Phase 0 a single grep-able evidence point that 'docs were reviewed against reality on 2026-05-28 and matched'."

patterns-established:
  - "Grounded-doc pattern: planning-side tree-notes file (Task 1) inspects live tree across N sections, feeds doc-writing tasks (Tasks 2+) verbatim with cite-real-paths-only discipline. Notes file is committed (audit trail) but NOT linked from user-facing docs."
  - "Flag-don't-normalize pattern for Phase 0.5: any inconsistency discovered during inspection is documented as a 'Known Inconsistency' callout in the affected doc + carried forward as Phase 0 input, never silently fixed (would muddy the zero-functional-diff exit-gate signal)."
  - "Human-verify checkpoint attestation: on user 'approved' resume, write a separate attestation.md capturing (a) the literal resume string, (b) timestamp, (c) paths reviewed, (d) per-step spot-checks the human was asked to perform per the plan's <action>. Committed as `docs({phase}-{plan}): record Task N human-verify attestation`."

requirements-completed: [AUD-05]

duration: 6 min
completed: 2026-05-28
---

# Phase 00.5 Plan 02: Documentation Summary

**docs/conventions.md (173 lines) and docs/dot_topics.md (157 lines) land as the documented baseline for Phase 0's structural refactor — derived from a 257-line live-tree inspection scratchpad, with attribute-prefix inconsistencies (rust/path.zsh, system/path.zsh.tmpl missing `executable_`) FLAGGED for Phase 0 rather than silently normalized; Teague approved both docs end-to-end against the actual tree.**

## Performance

- **Duration:** 6 min (active execution time; plan spanned ~3h wall-clock with a human-verify checkpoint pause)
- **Started:** 2026-05-27T22:32:00Z (Task 1 commit `a75cd43`)
- **Completed:** 2026-05-28T02:35:11Z (SUMMARY write)
- **Active execution:** ~6 min across both Claude sessions (Tasks 1-3 ~5min; Task 4 attestation + close ~1min)
- **Checkpoint pause:** Task 4 human-verify checkpoint paused execution until user provided "approved" resume signal
- **Tasks:** 4 (3 auto + 1 checkpoint:human-verify)
- **Files created:** 4 (2 user-facing docs + 1 tree-notes scratchpad + 1 attestation)
- **Files modified:** 0 (zero source-tree mutation outside docs/ — confirms zero chezmoi diff impact, per success criterion)

## Accomplishments

- **docs/conventions.md** lands with 173 lines covering all 9 RESEARCH-mandated sections (.chezmoiroot / .chezmoiscripts/ FLAT / .chezmoidata/ no-templating constraint / .chezmoitemplates/ partials / .chezmoiexternal.toml refreshPeriod=168h / dot_topics link / attribute prefixes / line-endings / canonical clean-check command) PLUS a "Known inconsistencies" callout flagging attribute-prefix divergences for Phase 0.
- **docs/dot_topics.md** lands with 157 lines documenting the REAL loader pattern (path → middle → completion) wired through `home/dot_zshrc.tmpl`, file-type conventions table, install.sh handling (NOT auto-sourced; invoked by `run_onchange_after_03-install-topics.sh.tmpl`), template files inside dot_topics/ (dotnet/executable_path.zsh.tmpl, system/path.zsh.tmpl), and a 4-line recipe for adding a new tool. Pitfall 22 (dot_topics undocumented) resolved.
- **8-section live-tree inspection** captured in `00.5-02-tree-notes.md` grounds Tasks 2-3 in current reality (enumerated 6 scripts in `.chezmoiscripts/`, 3 partials in `.chezmoitemplates/`, externals refresh policy, attribute-prefix examples, `.chezmoiroot = "home"` confirmed, `dot_zshrc.tmpl` loader logic line-range cited verbatim).
- **Both attribute-prefix inconsistencies** (rust/path.zsh, system/path.zsh.tmpl lacking `executable_` prefix) FLAGGED in both docs as known inconsistencies — DELIBERATELY NOT NORMALIZED in this phase because renaming would change destination file mode and break the zero-functional-diff exit-gate. Deferred to Phase 0 with the rest of structural refactor.
- **AUD-05 quick.sh section** now PASS (was PENDING in Plan 01); verified by running `bash .planning/phases/00.5-audit-documentation/checks/quick.sh` post-write — all grep + file-existence checks pass for both new docs.
- **Task 4 human-verify checkpoint** approved by Teague after end-to-end read of both docs against the actual tree. Resume signal "approved" + spot-checks recorded as committed attestation artifact (00.5-02-attestation.md) — provides Phase 0 a single grep-able evidence point.
- **Zero source-tree mutation outside docs/** — confirmed by post-write git diff: only `docs/conventions.md`, `docs/dot_topics.md`, and `.planning/` artifacts added; no chezmoi-managed source touched.

## Task Commits

Each task was committed atomically:

1. **Task 1: Re-inspect live tree and capture grounded content notes** — `a75cd43` (docs)
2. **Task 2: Write docs/conventions.md** — `2636709` (feat)
3. **Task 3: Write docs/dot_topics.md** — `ba0c7c8` (feat)
4. **Task 4: Teague accuracy review (human-verify) — attestation record** — `26652d1` (docs)

**Plan metadata:** _(appended after this SUMMARY is written)_

## Files Created/Modified

- `docs/conventions.md` (created, 173 lines) — 10 H2 sections covering inherited structural decisions; cites real file paths verbatim from tree-notes; cross-links to docs/dot_topics.md (section 6) and PITFALLS.md Pitfall 4 (section 8); ends with "Known inconsistencies (flagged for Phase 0 normalization)" callout.
- `docs/dot_topics.md` (created, 157 lines) — 6 sections covering the loader pattern (with `dot_zshrc.tmpl` line-range quote), file-type conventions table (path.zsh/completion.zsh/middle-pass/eval.zsh/install.sh), attribute-prefix inconsistencies flagged, template files inside dot_topics/, add-a-tool recipe, cross-link back to docs/conventions.md.
- `.planning/phases/00.5-audit-documentation/00.5-02-tree-notes.md` (created, 257 lines) — planning-side scratchpad NOT user-facing; 8 inspection sections from Task 1 grounding Tasks 2-3.
- `.planning/phases/00.5-audit-documentation/00.5-02-attestation.md` (created, 49 lines) — Task 4 resume-signal artifact with timestamp + paths reviewed + per-step spot-checks performed.

## Decisions Made

- **Docs describe REALITY, not RESEARCH/CONTEXT idealization.** Most notable: dot_topics.md documents the OBSERVED file-type set (path.zsh, completion.zsh, aliases.zsh, config.zsh, eval.zsh, install.sh) — a superset of the 4-type model CONTEXT cited ({path,eval,config,install}.zsh|sh). The doc flags this as superset-of-stated, not contradiction.
- **Attribute-prefix inconsistencies (rust/path.zsh, system/path.zsh.tmpl) FLAGGED, NOT normalized.** Renaming to executable_ would change destination file mode → break the zero-functional-diff exit-gate. Defer to Phase 0 with the rest of structural refactor. Both docs carry the flag so Phase 0 planning surfaces it.
- **`.chezmoiscripts/` "currently FLAT" is a load-bearing statement.** docs/conventions.md section 2 explicitly forward-references Phase 0's OS-nesting refactor — the doc's before-state is the reference point that change will read against.
- **Skip-renormalize policy from Plan 03 captured here.** docs/conventions.md section 8 (Line endings) encodes the empirical finding that Mac personal `.tmpl` files are already i/lf w/lf, fulfilling Plan 03's commit-message commitment ("canonical policy statement deferred to docs/conventions.md").
- **Canonical "is my repo clean?" command lives in conventions.md section 9.** `chezmoi diff -x externals` (per RESEARCH Pitfall A — externals weekly churn is a false alarm). Now plans 04, 05, 06 cite the doc instead of repeating the rationale.
- **Task 4 approval committed as separate artifact (not just commit message).** 00.5-02-attestation.md records the resume signal, timestamp, paths reviewed, and per-step spot-checks the human was asked to perform per Task 4 `<action>`. Gives Phase 0 a grep-able evidence point.

## Deviations from Plan

None - plan executed exactly as written.

The plan's three auto-tasks completed without triggering any Rule 1/2/3 auto-fix. The Task 4 checkpoint paused execution as expected; user provided "approved" resume signal; close-out proceeded per the plan's `<output>` spec.

The "describe reality, not idealization" pattern is itself the plan's intent (CONTEXT decision: "inspect the actual tree to derive content — do not invent conventions"), so the doc-content shape decisions are NOT deviations — they're the plan executing correctly.

---

**Total deviations:** 0
**Impact on plan:** None — plan was thoroughly grounded in Plan 01 harness + RESEARCH empirical findings + CONTEXT's "inspect tree first" discipline, leaving no surprises during execution.

## Issues Encountered

- **Pre-existing working-tree changes carried forward from before Plan 01:** `.planning/config.json`, `home/private_dot_config/nvim/lua/plugins/harpoon.lua`, `home/private_dot_config/nvim/lua/plugins/llm.lua`. Documented in Plan 01 + Plan 03 SUMMARYs as out-of-scope; this plan did NOT touch them; they remain unstaged after this plan. Not a blocker.
- **Wave 1 parallel-flight observation:** While Plan 02 was in flight, Plan 03 completed and landed `.gitattributes` + state-preview without conflict. The two plans touched disjoint paths (docs/ vs .gitattributes + .planning/phases/00.5/00.5-state-preview.md), so the parallel execution was clean — but this validates the wave-staging discipline of `00.5-CONTEXT.md` (zero apply-risk work runs in parallel before any source-tree mutation work).

## User Setup Required

None — no external services configured by this plan. Two new docs at repo root, zero chezmoi diff impact, zero machine-side changes.

## Next Phase Readiness

- **AUD-05 complete.** quick.sh section now PASS (was PENDING in Plan 01). Pitfall 22 (dot_topics undocumented) resolved.
- **Wave 1 complete.** Plans 01, 02, 03 all closed. Wave 2 (Plans 04 flameshot-removal + 05 packages-audit) can now proceed in parallel — both have docs/conventions.md as input (chezmoi no-auto-prune-empty-dirs behavior for Plan 04; `.chezmoidata/` no-templating constraint for Plan 05's packages.yaml work).
- **Phase 0 input artifacts seeded:** (a) "Known inconsistencies" callouts in both docs surface attribute-prefix divergences for the structural refactor; (b) `.chezmoiscripts/` "currently FLAT" baseline is the explicit before-state for the OS-nesting change; (c) canonical chezmoi-clean-check command (`chezmoi diff -x externals`) is the doc's anchor for Phase 0's exit-gate.
- **Tree-notes finding relevant to Plan 05:** `.chezmoidata/packages.yaml` was inspected as part of Task 1 section 1 enumeration (alongside tmux-languages.yaml, tmux-tools.yaml). Plan 05's candidate-removal list (`00.5-packages-candidates.md`, drafted by Plan 01) is the action artifact; Plan 02 contributed the convention statement that .chezmoidata/ files allow NO templating (loaded before template engine starts; chezmoi limitation).
- **No blockers.** Plan 04 + 05 can launch immediately.

## Self-Check: PASSED

Verified via `[ -f <path> ] && echo FOUND` for each file claimed, and `git log --oneline --all | grep -q <hash>` for each commit:

- FOUND: docs/conventions.md
- FOUND: docs/dot_topics.md
- FOUND: .planning/phases/00.5-audit-documentation/00.5-02-tree-notes.md
- FOUND: .planning/phases/00.5-audit-documentation/00.5-02-attestation.md
- FOUND: .planning/phases/00.5-audit-documentation/00.5-02-SUMMARY.md
- FOUND commit: a75cd43 (Task 1 — tree-notes)
- FOUND commit: 2636709 (Task 2 — conventions.md)
- FOUND commit: ba0c7c8 (Task 3 — dot_topics.md)
- FOUND commit: 26652d1 (Task 4 — attestation)

AUD-05 verified via `bash .planning/phases/00.5-audit-documentation/checks/quick.sh`: PASS (was PENDING in Plan 01).

---
*Phase: 00.5-audit-documentation*
*Completed: 2026-05-28*
