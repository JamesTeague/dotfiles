---
phase: 00.5-audit-documentation
plan: 06
subsystem: exit-gate
tags: [exit-gate, chezmoi-diff, drift-reconciliation, both-macs, AUD-04, mac-work, mac-personal, hot-patch, justified-escalation, employer-axis-gap]

requires:
  - phase: 00.5-audit-documentation
    provides: "Plans 01-05 closed (AUD-01/02/03/05 + SS-01 complete; AUD-04 Mac personal done in Plan 03); Wave 0 harness + state-preview scaffold + drift-reconciliation framework + flameshot baseline + packages-candidates manifest + approved-brew-bundle snapshot"
provides:
  - "Both-Mac `chezmoi diff -x externals` PASS — Mac personal EMPTY ✓, Mac work via single justified escalation (.zshrc NODE_EXTRA_CA_CERTS Bluebeam-specific corporate cert export, escalated to Phase 0 employer-axis design)"
  - ".planning/phases/00.5-audit-documentation/00.5-drift-reconciliation.md — Mac personal section (Plan 06 Task 1, .zshrc DEBUG=1 + Brother iPrint state-forge) + Mac work section (Plan 06 Task 5, localstack-cli hot-patch + flameshot residue cleanup + NODE_EXTRA_CA_CERTS escalation + start-aws-mcp.sh exact_bin discovery & relocation); 9 Phase 0 follow-ups total"
  - ".planning/phases/00.5-audit-documentation/00.5-exit-gate-report.md — load-bearing close artifact with both-Mac status, attestation block, anti-scope-creep checklist (6 items all green), Phase 0 input summary"
  - ".planning/phases/00.5-audit-documentation/00.5-state-preview.md — fully populated (Mac personal Plan 03 + Mac work Plan 06 Task 4 + Cross-machine deltas)"
  - ".planning/phases/00.5-audit-documentation/captures/mac-work-state.txt — raw Mac work scriptState (33 entries) + entryState (1680 entries) JSON dumps committed for Phase 0 provenance"
  - ".planning/phases/00.5-audit-documentation/captures/mac-work-task5.txt — full Task 5 reconciliation trail (pre/post-apply diffs + cleanup outputs + final gate capture)"
  - ".planning/phases/00.5-audit-documentation/recovery/start-aws-mcp.sh — verbatim 100-line recovery file extracted from Task 5 capture (NOT in chezmoi source); Mac work copies to `~/.local/bin/`"
  - "AUD-04 closed — Mac work CRLF pre-check ran (LF-clean) and Mac work chezmoi state captured + populated in state-preview; Mac personal half was already done in Plan 03"
  - "Reality correction landed in Plan 05: home/.chezmoidata/packages.yaml line 120 `localstack` → `localstack-cli` (Plan 05 candidate review's MISNAMED diagnosis was backwards; `localstack` formula is upstream-deprecated sunset 2027-04-12)"

affects:
  - "phase-0-refactor — inherits 9 explicit follow-ups (state-preview cleanup canonical commands, dry-run TTY brittleness, mas list invisibility + Apple ID audit, mas install non-interactive failure mode, state-forge pattern docs, AUD-02 LIGHT deferred inconsistencies, generate-gpg-key.sh is NOT run_once_, dot_zshrc.tmpl loader-order delta, **employer/site axis missing**, **chezmoi version-skew Mac work 2.69.4 vs Mac personal 2.70.4 + state-delete CLI behavior delta**, **home/exact_bin/ too strict for employer-local tooling**) AND one open escalation (`.zshrc` NODE_EXTRA_CA_CERTS Bluebeam corporate cert needs employer axis OR per-machine override)"
  - "phase-1-vaultwarden — no direct effect; secret-plane work is downstream of Phase 0 taxonomy"

tech-stack:
  added: []
  patterns:
    - "Capture-to-file-then-commit pattern for human-in-loop reconciliation: when the operator is at a different machine and can only paste limited output (here: Mac work physical access, no SSH per Bluebeam policy), redirect command output to `.planning/.../captures/<task>.txt`, commit + push from operator machine, pull + read on planner machine. Used 3× in Plan 06 (Task 3 state capture, Task 5 pre/post-apply, Task 5 final gate)."
    - "Foreground-vs-redirect distinction for chezmoi apply: when apply may prompt (sudo, kext approval, package conflict), run in foreground (visible prompts) NOT via heredoc-with-redirect (prompts disappear into the capture file, look like hangs). Used `ps aux | grep STAT` (look for `S+`) as the canonical stuck-on-tty diagnostic."
    - "Single-justified-escalation gate-pass pattern: when reconciliation would require introducing a new axis Phase 0 owns (here: employer axis for Bluebeam-specific corporate cert), document the entry as an explicit escalation in drift-reconciliation.md with justification, leave the diff non-empty for THAT entry only, and close the gate via the 'empty OR only justified escalations' criterion from Plan 06 success criteria."
    - "exact_-prefix-as-root-cause diagnosis pattern: when `chezmoi diff` shows a destination file as `deleted file mode 100755` with FULL content (not just an SHA), the file isn't in entryState — it's an `exact_<dir>` directive enforcing dir contents. `chezmoi state delete` won't help; either rename the source dir (drops the directive) OR move the destination file to a non-`exact_`-managed location."
    - "Bluebeam-specific tooling lives at `~/.local/bin/` (not `~/bin/`) on Mac work: `~/.local/bin/` is first on PATH (mise-bootstrapped) and NOT chezmoi-managed. Established as the canonical relocation target for employer-local scripts pending Phase 0's employer axis."
    - "zsh `--key=...` CLI flag parsing pitfall: when the heredoc-rendered command line splits `--key` from `=/path` (zsh EQUALS option kicks in on the `=`-leading second token), zsh tries to expand `=/path` as a command-path lookup and fails with `zsh: no such file or directory: --key=/...`. Workaround: space-separated `--key /path`. Surfaced on Mac work chezmoi 2.69.4; not reproduced on Mac personal 2.70.4 (zsh option set probably differs)."

key-files:
  created:
    - ".planning/phases/00.5-audit-documentation/00.5-drift-reconciliation.md (existed from Plan 06 Task 1; Task 5 added Mac work section + 4 new Phase 0 follow-ups #6-#9)"
    - ".planning/phases/00.5-audit-documentation/00.5-exit-gate-report.md — load-bearing close artifact"
    - ".planning/phases/00.5-audit-documentation/captures/mac-work-state.txt — Task 3 raw state capture"
    - ".planning/phases/00.5-audit-documentation/captures/mac-work-task5.txt — Task 5 full reconciliation trail"
    - ".planning/phases/00.5-audit-documentation/recovery/start-aws-mcp.sh — verbatim 100-line recovery file (NOT in chezmoi source)"
    - ".planning/phases/00.5-audit-documentation/00.5-06-SUMMARY.md — this file"
  modified:
    - ".planning/phases/00.5-audit-documentation/00.5-state-preview.md — Mac work section + Cross-machine deltas populated (Task 4)"
    - "home/.chezmoidata/packages.yaml — line 120 hot-patch (Task 5 mid-flight: `localstack` → `localstack-cli` Plan 05 reality correction)"

key-decisions:
  - "ESCALATE rather than fix-source for `.zshrc` NODE_EXTRA_CA_CERTS. Teague's distinction: `~/.certs/CAcerts.pem` is Bluebeam-specific, not all-work-machines-applicable. Gating under existing `{{ if not .personal }}` would carry Bluebeam config to every future work-axis machine. Phase 0's role taxonomy work owns the employer/site axis design. Until then: Mac work `chezmoi apply` keeps prompting, Teague keeps skipping, diff retains this single justified entry."
  - "Hot-patch the localstack rename rather than escalate to Phase 0. Plan 05 candidate review's MISNAMED annotation was a verification miss (didn't check whether `localstack-cli` ALSO exists in the same tap — it does, and `localstack` is upstream-deprecated). Reverted in commit `3725e90`. In-scope because it's a reality correction to Plan 05 packages-audit work, not a taxonomy decision."
  - "Move start-aws-mcp.sh to `~/.local/bin/` rather than touch `home/exact_bin/`. The `exact_` directive was the root cause of the persistent diff (NOT entryState orphan, as initially hypothesized). Removing `exact_` is a Phase 0 candidate decision (follow-up #9). `~/.local/bin/` is already on PATH via mise and chezmoi doesn't manage it — clean relocation, no Phase 0.5 source change."
  - "Skip `chezmoi state delete` on Mac work flameshot keys when `--key=...` syntax errored under zsh 2.69.4. Non-blocking: `chezmoi diff` reads source vs destination, both absent for flameshot, so diff is silent regardless of stale entryState. State hygiene is a Phase 0 follow-up (#7 + #8) — not a Phase 0.5 gate blocker. Space-separated `--key /path` form worked in the second pass."
  - "Mac personal exit gate re-verified post-rebase. Initial `chezmoi diff -x externals` was non-empty because the master rebase pulled `fa0a669 chore: discord is personal only now` (Teague's earlier Mac work commit) which mutated packages.yaml → `02-install-packages.sh` Pitfall D re-fire pending. Ran `chezmoi apply -x externals` to absorb (brew bundle no-op'd 81 deps). Mac personal returned to EMPTY ✓."
  - "Surgical commit scope held throughout — 6 commits spanning Task 1 reconciliation (Plan 06 prior session) + Task 4 state-preview + Task 5 capture + Task 5 hot-patch + Task 5 drift-reconciliation + Task 5 final capture + Task 6 phase-close. No working-tree pre-existing files (`.planning/config.json`, `harpoon.lua`, `llm.lua`) committed — surfaced in attestation block."

patterns-established:
  - "When the operator is on a different machine with limited paste budget, capture-to-file + git commit/push is the canonical transfer mechanism. Faster and lower-error than typing diff output back."
  - "Single justified escalation > forced fix-source when the fix requires inventing a new taxonomy axis. Phase exit gates accept escalations explicitly per Plan 06 success criteria; document the justification in drift-reconciliation.md and the gate report."
  - "Diagnose chezmoi-managed-directory drift by checking for `exact_` source prefix BEFORE chasing entryState. If the diff shows full content (not just SHA), the source has the file (impossible here) OR the `exact_` directive is enforcing dir contents."

requirements-completed: [AUD-04]

duration: ~3h active (across 2 sessions: 2026-05-27 evening Task 1; 2026-05-29 afternoon Tasks 2-6)
completed: 2026-05-29
---

# Phase 00.5 Plan 06: Exit Gate Summary

**Phase 0.5 closed via both-Mac `chezmoi diff -x externals` PASS — Mac personal EMPTY ✓, Mac work via single justified escalation (Bluebeam-specific `.zshrc` NODE_EXTRA_CA_CERTS export). 9 Phase 0 follow-ups captured (3 new from Task 5: employer axis missing, chezmoi version-skew + state-delete CLI delta, `home/exact_bin/` too strict). AUD-04 closed (Mac work CRLF pre-check LF-clean + chezmoi state captured + populated in state-preview Mac work section + Cross-machine deltas). Mid-flight reality correction landed (Plan 05 `localstack` rename reverted to `localstack-cli` because Plan 05's MISNAMED diagnosis was backwards — `localstack` is upstream-deprecated).**

## Performance

- **Duration:** ~3h active across 2 sessions (Task 1 on 2026-05-27 evening; Tasks 2-6 on 2026-05-29 afternoon)
- **Tasks:** 6 (3 auto + 3 human-verify checkpoint)
- **Reconciliations:** 5 distinct mid-flight findings reconciled (1 Mac personal in Task 1, 4 Mac work in Task 5)
- **Commits authored in Task 5/6:** 8 (rebase, state-preview Mac work, exit-gate scaffold, capture commits ×3, localstack hot-patch, drift-reconciliation + recovery + phase-close)

## Accomplishments

- AUD-04 closed (final Phase 0.5 requirement) — 6/6 requirements total (AUD-01..05 + SS-01)
- Both-Mac exit gate PASS verified — Mac personal EMPTY ✓ + Mac work justified escalation ✓
- `00.5-drift-reconciliation.md` complete with Mac personal (Task 1) + Mac work (Task 5) sections + 9 Phase 0 follow-ups
- `00.5-exit-gate-report.md` complete with attestation + anti-scope-creep checklist (6 items all green)
- `00.5-state-preview.md` fully populated (Mac personal + Mac work + Cross-machine deltas)
- Reality correction on Plan 05 landed without re-opening Plan 05 (commit `3725e90`)
- 3 new Phase 0 follow-ups surfaced (#6 employer axis, #7 chezmoi version-skew, #8/#9 chezmoi behavior + exact_ directive)
- Bluebeam-specific tooling pattern established: `~/.local/bin/` for employer-local scripts (already on PATH via mise; not chezmoi-managed)

## Task Commits

1. **Task 1: Mac personal drift reconciliation** — `416a985` (Plan 06 prior session 2026-05-27)
2. **Task 2: Mac work CRLF pre-check** — LF-clean, no commit
3. **Task 3: Mac work state capture** — `f59d8fe` (committed on Mac work, pulled to Mac personal)
4. **Task 4: state-preview + exit-gate scaffold** — `087cb7b` (after rebase + Mac personal re-apply for Pitfall D)
5. **Task 5: localstack hot-patch + Mac work reconciliation + recovery file + final capture** — `3725e90` + `b847fdd` + `baca2af` + `8a7c6d0`
6. **Task 6: phase close** — _(this commit)_

## Files Created/Modified

**Created (Plan 06):**
- `.planning/phases/00.5-audit-documentation/00.5-drift-reconciliation.md` (Task 1 prior session)
- `.planning/phases/00.5-audit-documentation/00.5-exit-gate-report.md` (Task 4)
- `.planning/phases/00.5-audit-documentation/captures/mac-work-state.txt` (Task 3)
- `.planning/phases/00.5-audit-documentation/captures/mac-work-task5.txt` (Task 5)
- `.planning/phases/00.5-audit-documentation/recovery/start-aws-mcp.sh` (Task 5; NOT in chezmoi source)
- `.planning/phases/00.5-audit-documentation/00.5-06-SUMMARY.md` (this file)

**Modified (Plan 06):**
- `.planning/phases/00.5-audit-documentation/00.5-state-preview.md` (Task 4: Mac work section + Cross-machine deltas)
- `home/.chezmoidata/packages.yaml` line 120 (Task 5: `localstack` → `localstack-cli` reality correction)

## Phase 0 Input — Open Escalation

ONE entry remains in Mac work `chezmoi diff -x externals` per the documented justification:
- `~/.zshrc` line — `export NODE_EXTRA_CA_CERTS=/Users/jteague/.certs/CAcerts.pem` — Bluebeam corporate Node TLS cert export. Awaits Phase 0 employer/site axis OR per-machine override mechanism (follow-up #6).

Until Phase 0 lands: Mac work `chezmoi apply` keeps prompting; Teague keeps skipping; diff retains this single justified entry.
