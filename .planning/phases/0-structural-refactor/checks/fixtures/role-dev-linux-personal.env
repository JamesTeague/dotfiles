# Fixture: role-dev-linux-personal
# Scenario: Linux dev install — role=dev, personal=true
# OS axis (linux) is controlled by the machine running the check; this
# fixture covers prompt axis only. Cross-OS rendering exercised at cutover time.
PROMPT_NAME=test
PROMPT_EMAIL=t@t
PROMPT_PERSONAL=true
PROMPT_ROLE=dev
