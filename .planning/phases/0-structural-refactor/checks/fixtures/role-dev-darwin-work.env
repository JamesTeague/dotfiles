# Fixture: role-dev-darwin-work
# Scenario: Mac work install — role=dev, personal=false
# OS axis (darwin) is controlled by the machine running the check; this
# fixture covers prompt axis only. Run on Mac work for work-overlay rendering.
PROMPT_NAME=test
PROMPT_EMAIL=t@t
PROMPT_PERSONAL=false
PROMPT_ROLE=dev
