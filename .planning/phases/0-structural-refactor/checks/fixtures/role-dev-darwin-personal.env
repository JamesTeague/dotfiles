# Fixture: role-dev-darwin-personal
# Scenario: Mac personal install — role=dev, personal=true
# OS axis (darwin) is controlled by the machine running the check; this
# fixture covers prompt axis only. Run on Mac personal for darwin rendering.
PROMPT_NAME=test
PROMPT_EMAIL=t@t
PROMPT_PERSONAL=true
PROMPT_ROLE=dev
